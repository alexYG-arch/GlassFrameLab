# macOS 可变尺寸 iOS 风格毛玻璃矩形

当前进展（2026-09-13）：用户已接受 U02-06 最大尺寸 RSS 已知问题，当前构建完成正式拖动与最终视觉验收，本机版本为 CLOSED_WITH_KNOWN_ISSUE。[验收报告](docs/缩放CPU修复与最终验收-UP02.md) 记录当前状态；以下原工作包失败及轮次按历史保留。


## 本版验收环境调整（用户确认，2026-09-11）

用户明确选择“本版限定当前本机环境，其余环境后续验证”。本版关闭与 WP-09 的验收环境限定为 Apple M4、macOS 26.5.2、内置 2x 屏；真实锁屏冷启动/解锁仍在本版验证范围。macOS 13、外接与跨屏、非 2x 和实际窄屏转为后续环境验证项，状态保留 `BLOCKED_VERIFICATION`，不写为通过或不适用。部署目标 macOS 13 不等于已验证最低系统兼容。此明确范围变更覆盖本版环境门禁，未放宽视觉、性能预算或代码检查要求。

版本：v0.3 · 更新日期：2026-09-11 · 状态：WP-00～04 已关闭；WP-05 第五轮的恢复问题经用户授权 UP-00 已修复验证，历史记录保留；视觉升级 UP-01 已关闭，WP-06 已关闭，WP-07 已关闭，WP-08 本机范围已关闭，WP-09 BLOCKED_STALLED（R5 缩放 CPU 超限）。细节见 docs/workpacks/UP-00.md、UP-01.md。

本次确认：第一版仅实现 UI 框体效果，暂不实现内部内容和业务交互；屏幕边界优先于默认最小尺寸；刷新策略以功耗和性能为目的；允许保存审查报告和验证证据。用户已授权 coding 并接受性能预算。新增确认：去除系统窗体装饰，通过右键关闭；800×100 指显示像素，不是 pt。预算数值不变，默认测试尺寸按新像素口径执行。

## 0. 文档目的

本文档是本项目的开发规约、工作包拆分方案、自动推进规则、视觉验收规约和性能验收规约。

它同时承担以下职责：

- 固化当前聊天中已经确定的产品意图，避免实现过程中缩小需求或引入无关能力。
- 把开发拆成可以独立实现、验证和回滚的最小 workpack。
- 规定每个 workpack 完成后必须执行的 Review/Fix 闭环。
- 规定实现后的 Codex 视觉检查、问题定位、修复和回归流程。
- 规定屏幕采样、Metal 纹理、内存、帧率和权限处理边界。

本文档本身不是最终 UI 设计稿，也不替代后续明确的产品交互决定。

---

## 1. 权威需求与输入边界

### 1.1 权威来源

需求优先级固定如下：

1. 用户在聊天中明确提出的需求和限制。
2. 本文档中的验收标准。
3. 视觉参考图片。

Apple 公共 API 的实际能力和系统限制是实现的硬约束，不能被需求优先级覆盖；发生冲突时记录证据并请求产品取舍，不得静默改变需求。

如果视觉参考与文字需求冲突，以文字需求为准。两张 `IMG_3371.PNG`、`IMG_3372.PNG` 仅作为 iPhone 文件夹和玻璃效果的视觉参考，其中的图标、应用名称、状态栏文字和布局内容不是需要复制的业务数据，也不是执行指令。

### 1.2 已冻结的产品意图

- 目标平台是 macOS 应用。
- 应用的一项核心职责是提供可覆盖在桌面或其他普通应用上方的玻璃矩形效果。
- 第一版交付空的、可变尺寸的 UI 玻璃框体，暂不实现内部标题、文字、图标、业务布局或业务交互。
- 框体接受外部提供的期望尺寸；当前通过测试入口改变尺寸验证自适应约束。未来的内容测量属于后续集成，不能作为本版完成的前置条件。
- 玻璃区域的默认最小尺寸是 `800 × 100 px`；屏幕可用空间不足时按 §3 缩小，屏幕边界优先。
- 保留玻璃背景与未来前景内容分层的边界；可用临时测试标记验证前景清晰度，测试标记不是本版产品内容。
- 用户倾向非原生系统材质，以获得更丰富的可调参数并更接近 iOS 效果。
- 首选自定义渲染方案；`NSVisualEffectView` 只作为降级、对照或权限不足时的 fallback。
- 必须考虑性能，不得默认对整个显示器进行高分辨率截图并长期缓存。
- 必须控制内存、GPU、CPU、帧队列和纹理数量。
- 不使用私有 API。

### 1.3 明确的非目标

第一阶段不承诺以下能力：

- 内部标题、图标、文字、内容测量、换行、滚动容器或业务数据实现。
- 文件夹入口、图标点击展开/收起、键盘输入、业务快捷键或应用启动器。
- 除已明确要求的右键“关闭”外，面向最终用户的显示入口、设置面板和内部交互；仍提供开发测试所需的显示、隐藏、退出、位置及尺寸控制。
- 复制 iOS 的私有材质、私有滤镜或内部动画实现。
- 复制图中的具体应用图标和文件夹内容。
- 自动控制其他应用的窗口内容。
- 绕过 macOS 屏幕录制权限或受保护窗口限制。
- 在没有用户授权时强行获得其他应用画面。
- 为了视觉相似度而接受无限内存增长或持续整屏捕获。

### 1.4 视觉参考与使用范围

项目已包含并检查以下参考图：

| 文件 | 可观察特征 | 本版用途 |
|---|---|---|
| [IMG_3371.PNG](IMG_3371.PNG) | 多个圆角容器在不同背景色下呈现半透明色彩、柔和边缘和局部高光 | 对照框体在深浅、冷暖背景下的材质表现 |
| [IMG_3372.PNG](IMG_3372.PNG) | 展开容器有大圆角、细亮边、柔和背景模糊及连续的颜色透射 | 框体材质的主要参考 |

两图原始尺寸均为 `1320 × 2868 px`。本次视觉检查使用工具缩放后的预览，未据此精确测量像素参数。静态截图不提供动画、功耗或真实模糊 sigma 的证据，不能从截图反推唯一滤镜参数。

只参考框体的圆角、模糊、透射色彩、边缘高光及阴影感；不复制标题、图标、状态栏、内部排版或整屏模糊。手机图中的框体比例不覆盖本项目 `800 × 100 px` 默认尺寸。WP-06 将初始视觉参数集中保存为候选预设，通过真实运行截图校准；这些工程参数不是用户已确认的精确设计值。

### 1.4.1 浅色背景与边缘深度补充（2026-09-11）

用户在复查 `IMG_3372.PNG` 后明确要求以下两种效果，均属于框体材质：

- **外侧暗色扩散光晕**：沿整个圆角轮廓向外柔和衰减，深色、低不透明度，水平与垂直偏移均为 0。作用是在白色、近白色及浅色背景上分离框体边界；不能只靠白色描边，也不能形成厚黑框或单侧落影。
- **内侧非均匀发光**：在细亮边内侧形成较宽、向中心衰减的柔和光带；沿周长的强弱与宽度平滑变化，圆角处连续，体现玻璃厚度。细描边与内发光必须独立控制，不能用均匀一圈白边代替。

参考图中可见细亮轮廓、内侧柔和明暗过渡及背景颜色透射；其中局部色块也来自背景，不能把这些色块固定画成材质光斑。单张截图不能证明系统使用何种滤镜，也不能唯一反推光照方向或参数。零偏移暗色外扩散是本次明确设计要求。

极浅背景上保留外侧暗色分离，避免继续整体提白。内侧亮部保留渐变；必要的内侧暗部只用于微弱厚度对比，不能把整块玻璃染灰或制造脏边。具体强度、宽度及沿周长变化曲线是待实际截图校准的候选参数。

外扩散所需透明留白按 §3 纳入窗口总尺寸和屏幕约束；玻璃主体仍为默认 `800×100 px`，不能为容纳光晕缩小主体，也不能新增可见外围底板。不能在光晕尾部留下矩形裁切边。完整检查记录见 [视觉参考复核](docs/visual-reference-review-2026-09-11.md)。

---

## 2. 技术基线

### 2.1 推荐技术栈

| 层次 | 技术 | 职责 |
|---|---|---|
| 窗口 | `NSPanel` / `NSWindow` | 无边框、跨 Space、置于其他应用上方、焦点和鼠标行为 |
| 测试宿主（按需） | SwiftUI + `NSHostingView` | 承载框体及可选测试标记；本版不实现内容布局 |
| 玻璃背景 | ScreenCaptureKit | 只采集矩形后方的局部区域 |
| GPU 管线 | Metal | 纹理处理、颜色、透明度、描边、高光 |
| 模糊 | `MPSImageGaussianBlur` 或 Metal shader | 可调模糊半径/sigma |
| 输出 | `MTKView` 或 `CAMetalLayer` | 低复制显示渲染结果 |
| 降级 | `NSVisualEffectView` | 无屏幕录制权限或采样不可用时提供可用外观 |

最低目标系统为 macOS 13+。若后续决定支持更低系统，必须单独建立兼容性 workpack，不得在当前 workpack 中隐式扩大范围。部署目标设置和编译成功不能替代 macOS 13 实机兼容验证。

### 2.2 窗口边界

窗口本身应透明，背景采样材质和内发光裁切在 `GlassContainer` 内部；外侧暗色扩散仅绘制到为它预留的透明边距。未来前景层及临时测试标记不得进入背景模糊管线。

本版用测试入口控制窗口显示、隐藏、退出、位置和期望尺寸，保证能重复验证及退出应用。产品框体没有系统标题栏、红黄绿按钮或额外窗口底板；通过右键菜单“关闭”退出当前框体，最后一个框体关闭时退出应用。底层 NSPanel 仅用于 macOS 承载，不使用系统窗体装饰。本版须接收右键事件，不能让整个框体鼠标穿透。其他业务键盘交互和显示入口留待后续集成。

基础窗口配置目标：

```swift
styleMask = [.borderless, .nonactivatingPanel]
isOpaque = false
backgroundColor = .clear
level = .floating
hidesOnDeactivate = false
collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
```

在 macOS 13+ 可评估加入 `.canJoinAllApplications`，但必须通过真实窗口测试确认其在目标场景下的行为，不能把它当作绝对置顶保证。

### 2.3 两种显示模式

#### 模式 A：局部玻璃矩形，第一版必做

```text
桌面/其他应用
       ↓
局部采样 + 模糊
       ↓
可变尺寸 GlassContainer
```

#### 模式 B：全屏背景模糊，后续可选

```text
全屏背景采样层
       ↓
较弱整体模糊
       ↓
局部玻璃矩形
```

模式 B 会显著扩大采样区域和性能风险，不能阻塞模式 A 的 MVP，也不能默认启用。

---

## 3. 响应式尺寸规约

### 3.1 最小尺寸

在屏幕空间足够时，玻璃区域的默认最小尺寸为：

```text
minimumGlassWidthPx  = 800 px
minimumGlassHeightPx = 100 px
```

本版输入为外部提供的期望框体尺寸 `requestedGlassSizePx`，单位为显示像素 px；未提供时使用默认最小尺寸。输入应为有限正数，无效输入使用默认值并记录诊断。内部内容测量由未来的调用方负责。

先按目标显示器的 backingScale 将期望像素换算为布局点，再应用屏幕约束。当前 2x 屏幕上的默认玻璃区域为 400×50 pt；1x 屏幕上为 800×100 pt。跨屏切换时保持像素口径并重新计算点尺寸。这里的像素指最终显示区域，不是背景降采样纹理大小。

未考虑屏幕上限前的期望尺寸（以下计算结果单位为 pt）：

```text
desiredGlassWidth  = max(800, requestedGlassWidthPx) / backingScale
desiredGlassHeight = max(100, requestedGlassHeightPx) / backingScale
```

本版不实现外置标题，不计入标题高度。为阴影保留的透明外边距必须计入窗口总尺寸，不能挤占已声明的玻璃区域尺寸。

### 3.2 最大尺寸

窗口尺寸必须受当前显示器可用区域约束。当前显示器按窗口相交面积最大者选择，相同时保持原显示器；无相交时选择最近的可用显示器。窗口跨越显示器时，本版约束在所选显示器内，不实现跨屏拼接采样。

```text
maxPanelWidth  = visibleFrame.width  - 48 pt
maxPanelHeight = visibleFrame.height × 0.80
maxGlassWidth  = maxPanelWidth  - horizontalShadowInsets
maxGlassHeight = maxPanelHeight - verticalShadowInsets
glassWidth    = min(desiredGlassWidth,  maxGlassWidth)
glassHeight   = min(desiredGlassHeight, maxGlassHeight)
```

`horizontalShadowInsets` 和 `verticalShadowInsets` 分别为两侧阴影预留空间之和，单位为 pt。可用空间不足以容纳正尺寸框体时暂停显示并报告几何不可用，不能创建负尺寸纹理或窗口。

约束优先级为：屏幕边界 → 最大尺寸 → 默认最小尺寸 → 期望尺寸/锚点。可用宽度不足 `800 / backingScale + 48 + horizontalShadowInsets` pt 或高度受限时，允许玻璃区域小于 `800 × 100 px`，这属于已确认的适配规则。测试至少覆盖 2x 屏幕上 `visibleFrame.width = 400 pt` 和 1x 屏幕上 `visibleFrame.width = 800 pt` 的情况。

最终窗口位置限制在 `visibleFrame` 内，水平方向保留 24 pt 边距；靠边变大时做最小幅度的位置调整。超过上限的尺寸输入直接夹取，并向调用方返回实际尺寸和受限状态；本版不实现换行、间距压缩或滚动容器。

### 3.3 尺寸变化与锚点

外部期望尺寸变化必须经过以下流程：

```text
外部期望尺寸变化（测试入口）
  ↓
50～100 ms 去抖
  ↓
计算最小/最大约束
  ↓
更新 NSPanel frame
  ↓
更新 Metal drawableSize
  ↓
协调更新 ScreenCaptureKit sourceRect 和输出 width/height
```

窗口扩展时必须保留一个明确锚点：

- 普通尺寸变化：优先保持当前窗口中心不变。
- 测试入口移动窗口后：以移动后的窗口中心作为后续尺寸变化的锚点，不跳回显示器中心。
- 以上锚点均服从屏幕边界约束；需要修正时采用最小位移。
- 本版不包含文件夹图标锚点及展开/收起交互。

不得形成以下布局循环：

```text
尺寸输入 → NSWindow 改尺寸 → 实际尺寸回传 → 再次提交相同尺寸 → 无限改尺寸
```

必须由一个独立的 `WindowGeometryController` 做去抖、变化合并和最终提交。

位置变化按当前刷新预算合并处理，不等待期望尺寸去抖。采样配置更新是异步过程，不能把上述顺序视作系统提供的原子提交；应将几何版本与帧元数据关联，仅展示与当前有效配置匹配的帧，并丢弃过期更新。输出尺寸采用有限档位复用，避免动画每帧重新分配纹理；不得为小框体预分配整屏档位。

---

## 4. 性能和资源规约

### 4.1 采样范围

默认只采集玻璃矩形后方区域，不采集整个显示器。

实际采集矩形应为：

```text
captureRect = glassRect + blurMargin
```

模糊边缘需要预留足够采样范围，避免矩形边缘产生硬切。初始实现可按约 `2～4 × blurSigma` 估算边距，并通过视觉测试调整。

### 4.1.1 坐标、输出尺寸与裁剪

- `glassRect` 先从窗口局部坐标转换为屏幕坐标，再转换为所选显示器的 ScreenCaptureKit 逻辑坐标；明确原点方向，不能直接混用 AppKit 坐标与采样坐标。
- `sourceRect` 使用 pt；`width/height` 和 Metal 纹理尺寸使用 px。`sourceRect` 只定义源区域，不能替代输出纹理大小配置。
- 若 shader 的 `sigma` 使用采样纹理像素，先计算 `sigmaPt = sigmaPx / (backingScale × sampleScale)`，再计算 pt 单位的模糊边距。`sampleScale` 为大于 0 且不超过 1 的背景采样比例；默认从 0.5 开始验证，不影响未来前景层的显示分辨率。
- 将加边距后的采样区域与所选显示器有效范围相交；输出尺寸按 `ceil(clippedSourceSizePt × backingScale × sampleScale)` 计算，或使用不小于此尺寸的最小复用档位。裁剪偏移与有效区域必须同步传给 shader，避免边缘拉伸和错位。
- 到达屏幕边缘时使用边缘延展等明确的模糊边界策略，不读取越界区域，不把另一块屏幕内容隐式拼接进来。
- 显式配置 `showsCursor = false`，避免把系统光标再次绘入玻璃。仅需要视频采样，不开启音频或麦克风采集。
- 排除本应用产生的玻璃及辅助窗口，窗口重建后同步更新过滤条件，避免递归反馈。

Apple 资料：[ScreenCaptureKit 流配置与帧元数据](https://developer.apple.com/videos/play/wwdc2022/10155/)、[sourceRect](https://developer.apple.com/documentation/screencapturekit/scstreamconfiguration/sourcerect)、[width](https://developer.apple.com/documentation/screencapturekit/scstreamconfiguration/width)。局部源区域和局部输出纹理是应用可设置和验证的边界，不将其表述为对系统内部所有合成资源分配的保证。

### 4.2 帧率和状态机

| 状态 | 目标行为 |
|---|---|
| 窗口隐藏、显示器休眠或会话锁定 | 停止采集和渲染循环；清理在途帧后不继续周期性刷新 |
| 窗口可见、位置固定且背景静态 | 采集刷新上限 10～15 FPS；无新有效背景帧时不重复模糊和绘制 |
| 窗口可见、背景持续动态变化 | 刷新上限约 30 FPS；性能和功耗预算不足时降频 |
| 窗口移动/调整尺寸 | 临时提升至最高约 30 FPS，结束后恢复背景状态对应的预算 |
| 权限拒绝/采样失败 | 立即切换 fallback，不重复高频重试 |
| 应用失去焦点但框体仍可见 | 按上述可见状态调度，不能仅因应用非活跃而冻结玻璃背景 |

这些帧率是节能调度上限，不是要求静态背景持续产生新帧。动态状态通过有效采样帧/变化元数据识别，设置状态转换去抖；不额外启动高频整屏轮询。暂停条件优先于可见状态；恢复可见或唤醒时先确认权限和几何，再重启采样。

本策略以功耗、性能与材质可用性的平衡为目的。应比较静态、动态和隐藏状态的 CPU/GPU 活动及能耗趋势；不能只以达到 30 FPS 作为优化成功的证据。

采集队列固定为有限深度，目标 `queueDepth = 3`。只处理最新帧，不能让旧帧排队导致延迟和内存增长。

### 4.3 纹理和内存

默认约束：

- 应用自建、可复用的中间处理纹理最多 4 张；在途 GPU 工作有明确上限，不能按帧持续增加纹理。
- 上述 4 张不包含 ScreenCaptureKit 队列持有的 IOSurface/CVPixelBuffer，以及 MTKView/CAMetalLayer 管理的 drawable。三类资源分别记录数量、尺寸、生命周期及可测内存，不能把中间纹理数冒充全链路总数；同一 IOSurface 的纹理视图不能重复计为新分配。
- 不缓存历史截图。
- 不生成整屏 `CGImage` 作为中间结果。
- 尽量保持 `CVPixelBuffer/IOSurface → Metal texture` 的低复制路径。
- 背景层允许在低于最终显示分辨率的采样纹理上进行模糊，再放大显示。
- 未来前景层与背景管线独立；临时测试标记变化不触发额外背景采样和模糊。

建议将性能预算记录为“相对基线”，而不是把整个应用的绝对内存误认为毛玻璃成本：

```text
启动后无玻璃窗口：记录 baseline
显示一个 800×100 px 玻璃矩形：记录 active delta
持续 5 分钟：检查 steady-state delta 是否继续增长
```

第一版验收目标：

- 800×100 px 矩形的采集输出及应用自建纹理按局部区域和模糊边距分配，不使用整屏大小的输出/中间纹理。系统内部资源只报告实测结果。
- 预热后持续运行 5 分钟，检查内存增量趋势、结束时残留资源及显示/隐藏循环后的回收情况。
- 窗口移动和尺寸变化记录帧间隔、采样到显示延迟及录屏结果，检查卡顿和旧帧拖影。
- GPU 帧时间按本应用每个有效渲染帧统计 P50/P95/最大值，不与采集等待时间或端到端延迟混为一项；只在动态测试场景考核约 30 FPS 的供帧能力。
- 若达不到预算，优先降低采样分辨率和帧率，而不是无限增加队列或缓存。

### 4.3.1 基准设备与预算冻结

2026-09-11 本机只读查询得到：MacBook Pro（Mac16,1）、Apple M4、24 GB 内存、macOS 26.5.2（25F84）。将其作为首个性能基准环境；这不是运行性能测试结果。本次查询未取得显示器分辨率和缩放数据，不推定外接屏或非 2x 环境已经具备。

WP-00 必须建立可复用的测量口径并记录基线：设备/系统、显示器逻辑与像素尺寸、缩放、电源状态、低电量模式、测试背景及工具版本。在实际采样/渲染开发前冻结唯一、可判定的数值预算，包括 GPU P95 上限、动态帧率容差、帧间隔/显示延迟上限、内存增长容差及功耗比较方法。原“8～12 ms 以内”不再作为模糊门槛。用户已接受 docs/workpacks/WP-00-performance-budget.md 中的预算；其数值已冻结，默认框体单位随本次确认修正为 800×100 px，不能假称目标已实测达标。

没有功耗计量能力时报告 CPU/GPU 活动及系统能耗指标作为代理证据，明确其不能证明精确瓦数。无法采样基线或冻结预算时记录 `BLOCKED_VERIFICATION` 或 `BLOCKED_INTENT_MISSING`，不能以“无明显问题”通过性能门禁。后续更改预算须说明原因并确认，不能在失败后自行放宽。

### 4.4 性能降级顺序

```text
降低刷新率
  ↓
降低背景采样分辨率
  ↓
降低模糊 sigma
  ↓
减少附加高光/颜色处理
  ↓
切换 NSVisualEffectView fallback
```

不得通过删除用户可见内容、降低文字分辨率或隐藏交互来掩盖性能问题。

---

## 5. Workpack 拆分决策

### 5.1 是否拆分

必须拆分。原因：窗口生命周期、响应式布局、屏幕采样、Metal 渲染、性能控制和视觉还原属于不同责任边界，合并开发会导致：

- 无法判断问题来自窗口、布局、采样还是 GPU 管线。
- 视觉问题和性能问题互相掩盖。
- 测试只能通过完整 App 才能执行。
- 任何一次参数调整都可能影响全部功能。

每个 workpack 必须是最小的、可独立描述和验证的变更单元。不得把“做完整毛玻璃效果”作为一个不可验证的大 workpack。

### 5.2 Workpack 总表

| ID | 目标 | 主要输出 | 前置条件 |
|---|---|---|---|
| WP-00 | 项目基线和验证工具 | 项目骨架、测试入口、性能/截图基线与预算口径 | 无 |
| WP-01 | 透明跨应用窗口 | `NSPanel`、层级、Space 和测试生命周期控制 | WP-00 |
| WP-02 | 框体尺寸约束 | 外部尺寸输入、800×100 默认最小尺寸、屏幕约束 | WP-01 |
| WP-03 | 局部采样服务 | 权限、局部 `sourceRect`、自身窗口排除 | WP-01 |
| WP-04 | Metal 静态玻璃管线 | 纹理输入、可调模糊和输出视图 | WP-02、WP-03 |
| WP-05 | 实时帧和性能控制 | 丢旧帧、节流、暂停、纹理复用 | WP-04 |
| WP-06 | iOS 风格视觉层 | 颜色、饱和度、描边、高光、阴影 | WP-04 |
| WP-07 | 框体动态变化 | 位置/尺寸变化、过渡动画和锚点 | WP-02、WP-05 |
| WP-08 | 降级和环境兼容 | 权限拒绝、保护窗口、多显示器、全屏 | WP-03、WP-05 |
| WP-09 | 最终视觉/性能验收 | 截图证据、性能报告、残留问题清零 | WP-01～WP-08 |

### 5.3 各 Workpack 的边界

#### WP-00：项目基线和验证工具

只建立可运行项目、基础日志、截图方式、性能采样方式和测试入口。不得提前实现完整毛玻璃。

验收：

- 应用可以启动和退出。
- 可以显示一个普通测试窗口。
- 能获得可重复的窗口截图。
- 能记录启动基线内存和一个简单帧率值。
- 按 §4.3.1 记录基准环境，建立测量口径并冻结性能预算；数值未确定时不得写成性能验证通过。
- 盘点 macOS 13、外接显示器、2x/非 2x、Space/全屏等验证条件，区分当前可测与待补环境；缺少后续环境不阻塞本包自身工具验收，但不得将对应场景标为通过。

#### WP-01：透明跨应用窗口

实现 `NSPanel` 容器和窗口行为，不接入真正的屏幕采样。

验收：

- 窗口无标题栏、背景透明。
- 能显示在桌面和普通应用上方。
- 可通过测试入口显示、隐藏、移动和退出；右键菜单“关闭”可操作且真正退出最后一个框体。默认框体为 800×100 显示像素；原 WP-00 校准窗口仅保留为显式测试模式。
- 全屏/Space 行为有测试记录。

#### WP-02：框体尺寸约束

实现外部期望尺寸输入、屏幕约束、位置夹取及实际尺寸回传，不实现内部内容测量和布局。

验收：

- 空间足够时，玻璃区域默认最小尺寸为 `800 × 100 px`。
- 外部尺寸输入增减时窗口稳定调整；空间不足时允许低于默认最小值，并返回受限状态。
- 靠边变大或切换显示器时，以最小位移修正位置，不越界。
- 阴影边距计入窗口总尺寸；不把占位文字、滚动或换行作为本包验收要求。

#### WP-03：局部采样服务

实现权限检查、ScreenCaptureKit 内容过滤、局部捕获和自身窗口排除。

验收：

- 只请求必要的屏幕录制权限。
- 采样区域随窗口位置和尺寸更新。
- `sourceRect`、输出像素尺寸、缩放和边缘裁剪符合 §4.1.1；光标不进入采样输出。
- 不出现捕获自身造成的递归画面。
- 权限拒绝可以被明确识别并传递给上层。

#### WP-04：Metal 静态玻璃管线

先处理单帧，不引入高帧率循环。实现最小可用的纹理输入、模糊和输出。

验收：

- 背景可见并被模糊。
- 模糊 sigma 可调。
- 用可选临时测试标记证明前景合成位于玻璃效果之上且清晰，不实现内部产品内容。
- 不生成整屏 bitmap 中间结果。

#### WP-05：实时帧和性能控制

实现帧节流、只保留最新帧、纹理复用、显示/隐藏暂停和性能日志。

验收：

- 采集队列有上限。
- 旧帧不会持续堆积。
- 隐藏窗口后停止采集和渲染；在途工作清理后无持续帧回调处理或周期性 GPU 提交。
- 5 分钟运行内存稳定。
- 调整窗口时不会显著增加纹理数量。
- 静态、动态、隐藏三类场景按冻结预算记录性能和功耗代理数据；框体仍可见时失去应用焦点不会错误停止背景更新。

#### WP-06：iOS 风格视觉层

只调整视觉参数和可见层，不改变采样生命周期或窗口交互。

验收：

- 模糊、饱和度、亮度、主题色和透明度可调。
- 圆角、边缘高光、细描边和阴影可独立控制。
- 深色/浅色背景下框体边界可辨识，透射和高光不过曝；临时测试标记保持清晰。
- 按 §1.4.1 实现低强度、零偏移的暗色外扩散，以及强弱/宽度沿周长连续变化的内发光；两者和细描边参数独立，外扩散不被窗口裁切。
- 真实运行分别检查纯白、近白浅灰、浅暖色、浅冷色、深色和多色背景；白色背景上的轮廓不能消失，深色背景上不能形成明显灰圈，内发光不能退化为均匀白框。
- 在默认 800×100 px 矮条形和较大框体上验证效果尺度；透明光晕边距不挤占玻璃主体，也不破坏既有屏幕约束。保存外扩散/内发光分别开关的对照截图。
- 视觉参数集中管理，不散落在多个视图和 shader 中。
- 参数预设可通过开发配置或测试入口调整；本版不要求产品设置面板。对照 §1.4 两张参考图记录匹配特征和残留偏差。

#### WP-07：框体动态变化

通过测试入口实现位置/尺寸变化及尺寸过渡动画，验证锚点、屏幕约束和采样同步；不实现文件夹展开/收起。

验收：

- 动画期间不产生布局循环。
- 尺寸变化优先保持当前中心，触及屏幕边界时按 §3 修正；测试入口移动后不跳回旧位置。
- 动画不会让采样区域短暂指向错误位置。
- 中断动画后状态仍然一致。

#### WP-08：降级和环境兼容

处理权限拒绝、受保护窗口、显示器变化、Retina 缩放、全屏和 Space。

验收：

- 无权限时界面仍可用，不显示黑色空洞。
- 受保护区域失败不会导致崩溃。
- 外接显示器和缩放变化后采样坐标正确。
- 跨屏移动时按 §3 选择目标显示器并约束窗口；本版不承诺跨两屏拼接显示。macOS 13 最低版本行为有真实测试记录。
- fallback 不依赖私有 API。

缺少必要设备或系统环境时，对应场景为 `BLOCKED_VERIFICATION`，不能用坐标单元测试替代真实显示器或最低系统版本验证。

#### WP-09：最终视觉/性能验收

只做问题清零、证据整理和最终审查，不新增产品能力。

验收：

- 所有视觉检查项都有截图或录屏证据。
- 所有性能目标都有测试记录。
- 没有未关闭的当前范围内 finding（包括 P2）。
- 完成最终 R/F 闭环后才可宣布完成。

---

## 6. 自动推进策略

### 6.1 是否自动推进

可以自动推进，但必须是“受门禁约束的自动推进”，不是无条件连续开发。

允许的推进条件：

1. 当前 workpack 的实现已经完成。
2. 当前 workpack 的 Review/Fix 循环在最多 5 轮内达到 `NO_FINDINGS`。
3. 该 workpack 的针对性测试、构建和必要的视觉检查均通过。
4. 下一 workpack 的 intent 不依赖未解决的产品决策。
5. 没有 `BLOCKED_*`、未验证的要求或开放的 P0/P1 finding。

满足以上条件后，Codex 可以在同一任务中自动进入下一 workpack，不需要每次询问“是否继续”。

### 6.2 必须暂停并请求用户决定的情况

- 需要改变已经冻结的产品意图。
- Apple API 能力与需求冲突，存在多个会改变产品体验的方案。
- 是否启用全屏背景模糊需要产品取舍。
- 需要降低最低系统版本或增加新的外部依赖。
- 需要用户授予屏幕录制权限时，应说明原因和 fallback 结果。
- 性能目标无法达到，且继续降低质量会明显影响视觉效果。
- 当前 workpack 的 Review/Fix 已达到第 5 轮且仍有开放 finding，立即进入 `BLOCKED_STALLED`，不得第 6 轮自动重试。
- 需要修改用户未授权的外部系统状态。

### 6.3 自动推进的禁止事项

自动推进时不得：

- 自动提交、推送、合并或发布。
- 自动创建用于任务调度/占用控制的锁文件、所有权文件或持久化调度状态；依赖工具生成的标准依赖锁文件不属于本条的调度锁。
- 自动开始与当前 workpack 无关的重构。
- 用“测试通过”替代视觉或性能验收。
- 用截图相似度不高作为理由随意扩大产品范围。
- 在当前 workpack 未达到 `NO_FINDINGS` 前开始下一包。

允许在项目内保存 canonical intent、finding 清单、R/F 轮次记录、修改映射、截图/录屏、性能测量和验收报告。这些文件用于证据留存及会话恢复，不赋予调度所有权，也不自动触发下一包。恢复执行时先核对当前实现与证据是否对应，不能把历史报告直接视作当前通过。记录格式保持简单，不要求新增数据库、控制器或持久化调度机制。

### 6.4 并行化策略

默认串行执行 WP-01～WP-08，因为窗口、采样和 Metal 管线存在强依赖。

可以并行的内容仅限于互不修改同一实现边界的只读任务，例如：

- Apple API 资料核对。
- 视觉参考测量。
- 性能测试方案设计。
- 独立的 shader 数学验证。

涉及同一窗口生命周期、同一渲染管线或同一布局状态时，不并行修改。

---

## 7. 每个 Workpack 的强制 R/F 闭环

本项目采用严格的、最多 5 轮的：

```text
Review 1 → Fix 1 → Review 2 → Fix 2 → Review 3 → Fix 3 → Review 4 → Fix 4 → Review 5 → NO_FINDINGS
```

### 7.0 Workpack 五轮上限

为遵守“修复后必须由新的 R_PASS 独立复核”的规则，本项目将“轮”定义为一次 `R_PASS`。如果该轮有开放 finding，则在同一轮结束前执行针对性 `F_PASS`；F 完成后进入下一轮 R。若该轮 R 已经是 `NO_FINDINGS`，则不需要执行 F，该 workpack 立即关闭。

硬性规则：

- 单个 workpack 最多执行 5 次 `R_PASS`，以及最多 4 次由前四次 Review 触发的 `F_PASS`。
- 第 1～4 轮：验证上一轮修复，发现并修复当前范围内的新问题。
- 第 5 轮：最终只读验证并完成收口。
- 第 5 轮结束仍有开放 finding 时，必须停止并报告 `BLOCKED_STALLED`。
- 第 5 轮发现问题时，不得再执行 F；因为修复后需要第 6 次 R 才能证明修复，当前 workpack 必须报告 `BLOCKED_STALLED`，等待用户决定是否以新 workpack 或调整后的范围继续。
- 不得以“问题很小”“后续 workpack 会覆盖”或“测试已经通过”为理由开启第 6 次 `R_PASS`。
- 不得通过合并 workpack、修改 intent、降低验收标准或删除测试来规避五轮上限。
- 自动推进只能发生在第 1～5 次 `R_PASS` 获得 `NO_FINDINGS` 的情况下。

五轮上限只限制同一 workpack 的 R/F 闭环，不限制用户明确改变需求后创建的新 workpack；需求改变时必须重新执行 Intent Freeze。

### 7.1 Intent Freeze

开始每个 workpack 前，必须写出当前 workpack 的 canonical intent，包括：

- 目标行为。
- 输入和输出。
- 默认值和单位。
- 边界条件。
- 错误处理。
- 性能约束。
- 视觉验收条件。
- 明确不做的事情。

不能用实现者自己的总结替代用户需求，也不能因为实现困难而悄悄缩小 intent。

### 7.2 Review R

R 阶段只读，不修改代码。必须检查：

- 用户需求是否有细节遗漏。
- 当前实现是否偏离 intent。
- 默认值、尺寸、单位和限制是否正确。
- 测试是否真的断言了需求，而不是只执行代码。
- 当前 workpack 是否引入了不必要的责任集中。
- 每个有效变更是否能映射到 intent atom 或已记录 finding。
- 构建、测试、静态检查、视觉检查和性能检查是否有证据。

### 7.3 Fix F

F 阶段只能修复最新 R 阶段列出的、属于当前 workpack 的 finding。

每个修复必须记录：

- `finding_id`。
- 对应的 `intent_atom_id`。
- 修改文件和符号。
- 为什么这是最小必要修改。
- 修复后的验证方式。

F 阶段不得顺便格式化无关文件、重构其他模块、添加新产品能力或修改 intent。

### 7.4 NO_FINDINGS 门禁

只有满足全部条件，当前 workpack 才能关闭：

- 所有 intent atom 都是 `IMPLEMENTED_VERIFIED` 或合理的 `NOT_APPLICABLE`。
- 没有 `MISSING`、`PARTIAL`、`DISTORTED` 或 `UNKNOWN`。
- 没有开放的当前 workpack finding。
- 变更范围完整可识别。
- 反向追踪完整：每个有意义的改动都对应需求或 finding。
- 构建和针对性验证通过。
- 必要视觉检查完成。
- 必要性能检查完成。
- 没有新的责任集中问题。

如果验证不能运行，必须使用 `BLOCKED_VERIFICATION` 或 `REVIEW_PARTIAL`，不能写成通过。

---

## 8. Codex 视觉校验与修复规约

### 8.1 视觉校验是强制门禁

在 WP-04、WP-06、WP-07、WP-08 和 WP-09 中，单元测试通过不代表完成。必须让 Codex 获取真实运行结果的截图或渲染输出，并进行视觉审查。

视觉检查必须基于当前实现的真实截图，不使用用户上传的参考图冒充实现结果。

### 8.2 每次视觉校验的固定步骤

```text
启动目标 App
  ↓
准备固定测试背景
  ↓
显示空玻璃框体（分层场景可添加临时测试标记）
  ↓
获取截图/录屏帧
  ↓
Codex 逐项检查
  ↓
记录 finding
  ↓
只修复当前 finding
  ↓
重新运行和截图
  ↓
确认视觉回归通过
```

如果视觉变化涉及窗口位置、尺寸或采样区域，必须在修复后重新获取截图，不能只依赖代码阅读。

### 8.3 固定视觉场景

至少检查以下场景：

| 场景 | 检查内容 |
|---|---|
| V-01 | 空间足够时 800×100 px 默认最小玻璃区域，以及窄屏下的缩小适配 |
| V-02 | 外部宽度输入增减、最大宽度夹取与靠边位置修正 |
| V-03 | 外部高度输入增减、最大高度夹取及阴影边距 |
| V-04 | 临时测试标记位于背景模糊之后，验证前景分层；产品内部内容不在范围内 |
| V-05 | 彩色桌面背景下的透射、饱和度和色调 |
| V-06 | Finder/浏览器等普通窗口上方 |
| V-07 | 深色背景与浅色背景 |
| V-08 | Retina 2x 和非 2x 缩放 |
| V-09 | 窗口移动和动态调整尺寸 |
| V-10 | 框体尺寸过渡与动画中断，不包含文件夹展开/收起 |
| V-11 | 屏幕录制权限拒绝后的 fallback |
| V-12 | 外接显示器/全屏/Space |

V-08/V-12 必须记录实际显示器、缩放及环境。缺少所需设备或系统时标记 `BLOCKED_VERIFICATION`，不能为关闭 WP-09 而标为 `NOT_APPLICABLE`。几何计算的合成输入可以辅助覆盖窄屏边界，不能代替真实窗口截图及跨屏行为验证。

### 8.4 Codex 视觉审查清单

每张截图必须检查：

- 玻璃矩形边界是否正确。
- 圆角是否有锯齿、断裂或裁剪错误。
- 背景是否真的发生模糊，而不是只叠加半透明颜色。
- 矩形内部背景是否连续，没有硬切边。
- V-04 临时测试标记是否清晰；其他场景不要求存在内部内容。
- 边缘高光是否连续且不过亮。
- 描边是否出现双线、断线或不均匀。
- 阴影是否偏移、过重或被裁剪。
- 尺寸增减后是否出现背景空洞、拉伸错位或裁切。
- 采样画面是否包含自身窗口反馈。
- Retina 缩放下框体边缘和测试标记是否仍然锐利。
- 深浅背景下框体边缘是否可辨，高光和透射是否保持连续。

### 8.5 视觉 Finding 的格式

```yaml
finding_id: VIS-001
intent_atom_id: <当前框体需求或测试需求 ID>
category: VISUAL_GEOMETRY | VISUAL_LAYERING | VISUAL_BLUR | VISUAL_COLOR | VISUAL_PERFORMANCE
priority: P0 | P1 | P2
scene: V-01
expected: <期望视觉行为>
observed: <截图中实际观察到的行为>
evidence: <截图路径、帧号或复现步骤>
likely_area: <几何/窗口/采样/Metal/测试前景层>
minimal_fix: <最小修复方向>
recheck: <修复后必须重新检查的场景>
```

### 8.6 视觉修复原则

- 一次只修当前 finding 对应的最小范围。
- 不通过隐藏内容、降低清晰度或改变需求来消除问题。
- 不用系统材质临时遮盖自定义管线中的错误，除非该 finding 明确属于 fallback。
- 修复后必须重新运行并截图。
- 普通视觉检查遵守当前 workpack 的五轮 R/F 上限；最终效果的 Codex 模型视觉判断另行遵守下方的三次视觉判断上限。不得为了继续视觉调参而绕过任一上限。

### 8.7 最终效果的模型视觉判断：三次 Loop 上限

WP-09 的最终效果判断必须由 Codex 基于真实运行截图或录屏帧独立完成，最多执行 3 次视觉判断。它纳入当前 `R_PASS`，同时累计视觉判断次数；不是可在 R/F 之外额外执行的修复通道。

一次视觉判断定义为对当前版本的一组最终场景截图/录屏帧进行审查；一组可覆盖多个场景，不按单张截图计数：

```text
在当前 R_PASS 获取最终效果截图/帧
  ↓
Codex 模型判断视觉问题
  ↓
记录 VIS finding 或无视觉问题结论，并累计一次视觉判断
```

规则：

- 第 1 次：判断最终效果是否达到几何、层级、模糊、颜色和动态视觉要求；截图不能替代功耗/性能测量。
- 第 2 次：验证第 1 次修复，并判断是否仍存在视觉问题。
- 第 3 次：最终只读视觉门禁，只能输出通过或阻塞，不执行 F。
- 第 3 次仍有开放 `VIS-*` finding 时，必须报告 `BLOCKED_STALLED`，不得执行第 4 次视觉判断 loop。
- 如果第 1 或第 2 次已经没有视觉 finding，则提前结束视觉判断 loop。
- 视觉修复仍必须映射到当前 workpack 的 finding 和 intent atom，不能借视觉 loop 引入新的产品能力。
- 最终视觉判断的截图必须来自当前实现，不能使用参考图作为通过证据。

只有视觉判断次数小于 3 且当前 `R_PASS` 次数小于 5 时，开放 finding 才能进入对应 `F_PASS`；修复后由下一次 R 和视觉判断重新审查。两个上限任一耗尽且仍有 finding，立即 `BLOCKED_STALLED`。视觉通过仅表示该检查项通过，整个 workpack 仍须满足其他 R 门禁。

例如：`R1/视觉1 → F1 → R2/视觉2 → F2 → R3/视觉3（只读）`。若视觉1发生在 R5，即使视觉次数仍有余量，也不得再修复。视觉通过后任何影响可见结果的修改都使旧视觉结论失效，必须在剩余计数内复核；计数耗尽则阻塞，不沿用旧截图通过。

---

## 9. 自动化验证矩阵

### 9.1 单元/逻辑验证

- 最小尺寸计算。
- 最大尺寸、窄屏例外和期望尺寸夹取。
- 阴影外边距与玻璃尺寸的分离。
- 锚点计算。
- 外部期望尺寸变化去抖及相同尺寸去重。
- Retina 点/像素换算。
- 采样矩形边距、屏幕边缘裁剪和输出纹理尺寸计算。
- 主采样显示器选择及跨屏位置约束。
- 帧节流和状态机。
- 权限状态转换。

### 9.2 集成验证

- `NSPanel` 显示和隐藏。
- ScreenCaptureKit 权限流程。
- 当前窗口排除。
- 采样矩形更新。
- 光标关闭、自身辅助窗口排除及过期采样帧丢弃。
- Metal 输入和输出。
- 窗口尺寸变化联动渲染尺寸。
- 关闭、后台、全屏和 Space 状态。

### 9.3 性能验证

每个性能测试需要记录：

```text
设备型号
macOS 版本
屏幕分辨率和缩放
玻璃区域尺寸
场景状态（静态/动态/隐藏）及目标刷新上限
有效采集帧率、实际渲染帧率及帧间隔
GPU 帧时间 P50/P95/最大值
采样到显示延迟及测量方法
CPU 占用趋势
GPU 活动、能耗指标及工具/计量限制
电源状态、低电量模式和测试背景
启动 baseline 内存
显示后增量内存
预热时长、5 分钟 steady-state 内存序列及增长斜率
显示/隐藏循环后的残留资源与回收结果
应用中间纹理、采集缓冲、drawable 的分别统计及队列深度
实际结果与 WP-00 冻结预算的逐项比较
```

至少执行：

- 屏幕空间足够时的 800×100 px 单窗口，以及受限屏幕上的实际适配尺寸。
- 1600×300 px 大窗口：仅在 §3 上限允许时执行原尺寸实机测试；否则测试被夹取后的尺寸并记录。需要验证原尺寸时另用足够大的显示器，不能声称夹取结果证明了 1600×300 性能。
- 连续移动 30 秒。
- 连续调整尺寸 30 秒。
- 显示/隐藏循环 20 次。
- 持续运行 5 分钟。
- 使用相同框体、背景和环境分别记录静态、动态、隐藏场景，验证降频减少无效工作；没有新帧时不得以重复提交 GPU 工作凑帧率。

测试报告必须区分实际测量、代理指标和未测项目；WP-00 的基线不是 WP-05/WP-09 的运行结果，参考截图也不能证明性能达标。

---

## 10. 发现问题后的处理规则

### P0

影响崩溃、权限安全、严重内存增长、无法关闭窗口、自身捕获递归或核心需求缺失。

处理：立即停止推进，在 §7/§8 剩余轮次内修复并重新 Review；轮次耗尽时报告 `BLOCKED_STALLED`，不能绕过上限。

### P1

影响核心视觉、响应式尺寸、跨应用显示、明显性能问题或重要场景兼容性。

处理：当前 workpack 必须修复，不得带入下一包。

### P2

局部视觉偏差、轻微间距问题、非核心设备差异或不影响当前验收的维护问题。

处理：如果属于当前 workpack，仍需关闭；如果属于未来能力，记录为后续任务但不得混入当前修复。

### 阻塞状态

使用以下明确状态，不使用模糊的“稍后处理”：

- `BLOCKED_INTENT_MISSING`
- `BLOCKED_INTENT_CONFLICT`
- `BLOCKED_VERIFICATION`
- `BLOCKED_OUT_OF_SCOPE`
- `BLOCKED_STALLED`
- `REVIEW_PARTIAL`

---

## 11. 最终完成定义

项目只有在以下条件全部满足时才能宣布完成：

- 局部玻璃矩形可在桌面和普通应用上方显示。
- 空间足够时默认玻璃区域不小于 `800 × 100 px`；窄屏/低高度例外按 §3 适配，窗口不越界。
- 外部期望尺寸输入能触发稳定的框体尺寸变化、锚点保持及必要的位置修正。
- 交付仅包含 UI 框体效果；内部内容、内容测量、文件夹展开/收起及业务交互不作为验收要求。
- 背景模糊管线与未来前景层分离，临时测试标记已验证清晰度。
- 自定义模糊参数、透明度、色调、饱和度、圆角、描边和阴影可配置。
- 不依赖私有 API。
- ScreenCaptureKit 只采样必要局部区域。
- 当前窗口不会被自身捕获。
- 帧队列、纹理数量和内存增长受控。
- 静态降频、隐藏停止、动态受预算约束的调度行为有性能及功耗代理证据；具体门槛按 WP-00 冻结预算判定。
- 无权限时存在可用 fallback。
- 视觉场景 V-01～V-12 已检查并关闭 findings。
- 所有 workpack 都完成独立 R/F 闭环并达到 `NO_FINDINGS`。
- 最终 WP-09 的构建、测试、视觉和性能证据齐全。
- 缺少 macOS 13 或必要显示器环境时如实报告验证阻塞，不将本机通过扩大为全部兼容性通过。

---

## 12. Codex 执行协议

Codex 每次开始一个 workpack 时，必须先输出：

```text
WORKPACK_START
id: WP-xx
intent: <本包唯一目标>
in_scope: <本包允许修改的文件/模块>
out_of_scope: <明确不修改的内容>
acceptance: <可观察验收条件>
validation: <需要执行的检查>
```

实现完成后必须执行：

```text
R_PASS 1
  ↓
若有 finding：F_PASS 1
  ↓
R_PASS 2
  ↓
若有 finding：F_PASS 2
  ↓
R_PASS 3
  ↓
若有 finding：F_PASS 3
  ↓
R_PASS 4
  ↓
若有 finding：F_PASS 4
  ↓
R_PASS 5（最终门禁）
  ↓
NO_FINDINGS 或 BLOCKED_*
```

`NO_FINDINGS` 之前不得开始下一个 workpack；第 5 轮未达到 `NO_FINDINGS` 时只能停止，不能自动开始下一个 workpack。WP-09 的最终模型视觉判断纳入对应 R_PASS，并同时遵守最多 3 次的视觉判断上限，计数关系按 §8.7 执行。

下一个 workpack 开始前必须输出：

```text
WORKPACK_CLOSE
id: WP-xx
status: NO_FINDINGS
validation: PASS
visual_check: PASS | NOT_REQUIRED
performance_check: PASS | NOT_REQUIRED
open_findings: NONE
```

本项目的默认自动推进策略是：在同一 Codex 任务内按 WP-00 → WP-09 串行推进；遇到产品决策、权限、环境或重复失败时暂停并请求用户方向。自动推进不包含 commit、push、发布或无关重构。
