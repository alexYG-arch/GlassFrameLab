# 背景自适应深色材质升级方案 v1.3

日期：2026-09-14。状态：**VISUAL_CONFIRMED / MACOS_PACKAGE_VERIFIED / OS_PERMISSION_RECOVERY_PENDING**。用户已采用 v1.9.3 并授权重新打包；新包的短启动仍检测到缺少屏幕录制权限。最小短测不能代替功能/视觉验收，未验证环境及已知问题继续保留。

修正范围：减少的是 RSS 专项验证成本，功能覆盖不缩减。矩形移动/缩放期间仍冻结高光及内部映射相位、保持显示、结束后继续。正常应用启动的权限链路必须单独验证，直接执行开发二进制通过不能替代。

## 当前 Light 优化入口（v1.9.3）

用户要求“Light 下不显示线，只显示柔光映射”。当前源码 Light 删除流动细线，仅保留增益 0.75 的原蓝紫柔光；Dark 细线与映射、基础玻璃描边保持原样。中间亮度沿已有连续系数退去细线。当前实现、截图与验证见 [v1.9.3 试验记录](Light仅柔光映射试验-v1.9.3.md)，**USER_VISUAL_CONFIRMED / MACOS_PACKAGE_VERIFIED**。新包正常启动检查显示 `permission_required`，需授予屏幕录制权限后才能使用实时材质；真实授权恢复未验证。

这是对旧方案“Light/Dark 均显示细线”的明确覆盖；仍共用路径、色段、运动与冻结规则，其他基础隔离及功能要求继续有效。下文历史参数不覆盖本入口；用户本次已明确授权，应用 ZIP 及启动链接已更新。

## 执行约束修订（2026-09-14，优先于下文历史候选）

1. **保护 Light 基线**：默认 GlassStyle、几何、透明度及合成输出不因 Dark 升级而调整。固定相同输入，流光关闭，比较升级前和当前 Light 输出；任何差异先查明，不用加深浅色细边、增加底色或改变基础阴影补偿。没有通过对照就不能标记 Light 回归通过。
2. **流光保持同色调适配**：以品牌色 `#3B78DD` 的 sRGB HSV 色相 217.4074° 为蓝色头部基准，Light 与 Dark 均保留紫色尾迹。最新用户要求 Light 提高饱和度和明度，覆盖此前降低明度的候选；Light 合成前候选蓝为 `#0058EB`（S=1、V=0.92），Dark 仅旋转蓝色色相、保持原 S/V。外沿与内侧柔光共用适配后的颜色，分别保留细线渐隐和宽柔光包络。调色在明确的 sRGB/线性转换后进行，不能把网页色值直接作为线性 RGB。
3. **适配独立性**：流光只读已有背景统计；不修改 GlassStyle、基础 Dark 端点或阈值。即使基础 Dark 自适应关闭，开启流光仍能独立适配；关闭流光后恢复同一基础输出。复用现有统计 buffer 和渲染 pass，不为流光新增屏幕采集或逐帧重新统计。
4. **验收不能降级**：RSS 缩减只适用于资源专项；代码编译、单元检查、功能运行、视觉检查分别记录。未完成/失败保持未通过，禁止因时间、实现困难或已有计数正常而放宽文档要求。
5. **仅源码开发**：只修改代码、编译和验证。未经另行要求，不生成 ZIP、不替换交付 app；开发进程通过不能冒充正常 app 授权链路通过。

必须补齐的直接证据：相同输入 Light 基线对照；Dark 在深背景上变化而 Light 不变；浅/深底流光颜色及渐隐首端/柔光视觉；背景变化时实时透射；开关、拖动/缩放冻结及恢复、once/隐藏/退出；异常路径状态与恢复。资料中 Magic UI Border Beam 的配色仅参考对比处理方法，不替换本项目色相。

### 历史实现候选（v1.8，现由文首 v1.9.3 覆盖）

- **Light 与 Dark 共用几何与色段**：周期 7 秒、尾长占周长 0.20、顺时针、头部渐隐范围、尾迹衰减指数 2.0、蓝紫过渡 `smoothstep(0,0.9,t)`、柔光扩散包络均相同。禁止因浅底调色独立延长紫段或尾迹。移动/缩放冻结规则不变。
- 线条共用 `max(aa*0.75,0.3)` 高斯尺度；本机 2x 为 0.375 pt，撤销 Light 独立加粗 0.75 pt。实际图像中的强度和宽度感知仍受背景影响。
- 头色以品牌蓝 #3B78DD 色相 217.4074° 为基准；Light S=1、V=0.92，合成前候选 #0058EB；Dark 保留原 S/V，仅使用共同品牌蓝色相。紫色沿同一混色曲线出现，不单独改变位置。
- Light 在合成时仅把流光颜色相对最高通道的色差放大 1.30 倍，再将合成结果限于有效预乘色域；基础背景不做全局增饱和。移除 Light 流光自身额外的中性压暗，基础玻璃阴影不改。该增纯不能改变 alpha、几何或色段位置。
- Light 保留已要求的弱化透明度：线条幅度乘 0.75，柔光映射增益 1.33；颜色与透明度是背景适配细节，不改变路径、长度、色段或时序。
- 复用原 8×8 采样和原 float4 空闲分量；基础 Dark 的 L、D、w 不变。流光浅底程度 `smoothstep(0.12,0.60,M)`，流光独立适配，不新增采集或统计 buffer。
- 浅底预乘 sRGB 值在渐变混合前限制到 alpha 范围，避免渐隐后上侧被裁成白色。移除 v1.6 的额外接触覆盖及接触着色；禁止用新增内侧色带遮住白条。基础材质关闭流光时保持原像素输出。
- 必须检查整条线横截面的首尾同步渐隐，不能只看中段，或以“弱端亮度必须持续变暗”强迫增加接触阴影。亮度、颜色覆盖、线宽和实际截图联合判断。

以上是实现候选，不代表用户已确认精确视觉。几何依据见 [v1.7](统一流光几何与渐隐修复-v1.7.md)，最新颜色验证及限制见 [v1.8](浅底流光增纯修复-v1.8.md)。

## 本轮范围校正

用户仅要求简化 RSS 等资源专项验证。撤销将该要求扩大为缩减实现与视觉验证的解释；以下修订覆盖旧的“最小成本验证”约束。

用户随后校正运动规则：**移动/缩放期间冻结流光，保留边缘高光及关联柔光/阴影；不隐藏、不清除。** 该校正替代此前“运动期间不出现/全部关闭”的讨论。这里只冻结流光的时间推进，不冻结窗口画面或真实背景更新。

## 视觉方向确认记录（2026-09-14）

用户对最新柔光预览回复“welldone，更新升级文档”，据此将 [流光柔光映射概念图 v0.2](previews/border-flow-soft-spill-v0.2.png) 记录为 **visual_direction_confirmed**，作为后续实现的视觉参考。

已确认：保留所选首张流光图的外沿亮头、青紫尾迹和炫酷感；内部映射采用无清晰边界的柔光发散，映射光头沿流动方向也必须模糊展开；外沿线条主体保持清晰，首端也采用渐隐过渡；不得出现第二条内线、完整内圈、硬截止边或窄暗带。局部柔光与弥散阴影绑定流光位置及衰减，随总开关一起退出，基础材质参数保持独立。

确认的是视觉方向，不将概念图的几何误差写入产品规格。主体仍为 800×100 物理像素；精确光效参数、完整动画、开关恢复和 CPU/GPU/RSS 性能仍待实现验证。后续按已确认方向标定，无需重新询问同一方向；若出现实质视觉取舍，再提交差异供用户确认。后续用户已通过“开始更新”授权实现；当前进度见 [实现与最小验证记录](自适应与流光实现记录-v1.3.md)。

## 问题与目标

用户希望框体后方为深色、黑色背景时，玻璃的颜色与光效随之调整，避免现有白色/浅色光效突兀。采用框体后方真实背景作为主要输入，不依赖对其他应用“是否开启 Dark Mode”的判断。系统为浅色但背景很暗时仍可显示深色玻璃，系统为深色但背后是白色页面时仍保留浅色背景可辨识性。

现有 ScreenCaptureKit 已捕获框体背后的局部像素，Metal 在真实背景纹理上模糊与合成。透射颜色已经随背景变化；缺口在光效参数：GlassStyle 的 innerColor 固定白色、innerStrength=0.16、strokeOpacity=0.40，fragment shader 也将边缘固定混入白色。白色 tintOpacity 仅 0.008，不能将全部突兀感都归因于填色。

代码依据：[GlassStyle 默认值](../Sources/LabSupport/GlassStyle.swift) 与 [GlassRenderer 合成](../Sources/GlassFrameLab/GlassRenderer.swift)。

本次目标是深背景上减弱白色高光和灰圈，保留透明感、圆角和适度深度；浅色背景保持目前已验收的外扩散分离效果。仅调整框体材质，不增加内部内容、业务 UI、系统主题开关或窗口装饰。

## 本次调整：视觉协调性优先，参数经渲染后确定

按本轮相邻上下文，将用户所说“上一版”记录为 **v0.2**，仅作为视觉协调性参考，不记为精确效果验收通过。[参考图片](previews/adaptive-glass-concept-v0.2.png) 保留其生成来源；图中比例、背景透射位置及光效仍有偏差。v0.3 的刻意压暗未获得更好的整体协调性，不作为继续削弱光效的依据。

保留 v0.2 柔和连贯的轮廓、可感知的内侧厚度与背景融合。深色玻璃可以有适度亮边和局部提亮，不能把“越暗越好”作为优化目标。整体灰雾、背景错位、均匀厚灰圈及偏移阴影不属于应保留的风格。

后续顺序调整为：

1. **静态标定（已采用原生单帧核对，独立离屏工具未另建）**：独立离屏预览复用现有合成公式、线性颜色处理与背景坐标。固定原始背景输入，只改变材质参数；不接入实时采集、不修改当前已验收应用。主体严格 800×100 物理像素；在 2x 下含既有外扩散留白输出 824×124 像素。放大展示另标倍率。
2. **视觉选择（概念方向已确认，最终原生画面待用户 review）**：输出同一背景的现有材质、原建议端点、协调性候选对照，以及必要的原图。背景几何一一对应；测试线条用于定位，不写入产品框体。另提供自然背景检视，避免只根据测试线条判断协调性。
3. **端点与自适应（已接入候选参数）**：先保持原 8×8 判定公式为独立候选，用固定端点排除判定系数对材质调色的干扰，再验证整框自适应。不用修改亮度判定阈值来掩盖材质本身的缺陷。
4. **流光独立标定与接入（已实现，已修正柔光头部）**：按已确认的柔光发散方向标定关闭、直边、转角的局部明暗映射，再检验完整一圈和开关恢复；基础材质与流光分开记录参数。
5. **功能与资源分开验证（已重开）**：执行下述功能覆盖；RSS 只做短测。正常启动故障未闭合前不得宣布交付通过。

静态阶段优先调节内光强度、细边不透明度和两者的平衡；一次只改变一个参数或明确的一组关联参数。基础材质标定暂不改变 sigma、内光宽度、圆角、阴影范围。新增流光作为下述独立可选模块标定，不通过改写基础材质参数实现。若原候选显得单薄，允许适度回调内光/细边，记录真实渲染数值后再确认，不从生成图片反推精确参数。

## 视觉策略

整块框体使用一个自适应系数，不逐像素切换主题，以免形成斑块。对框体主体投影区域统计限制高亮样本贡献后的亮度及深色采样占比；忽略外扩散透明边界、模糊纹理桶的未使用区域及屏幕无关内容。

初版采用连续亮度映射，不设置“刚低于阈值就突然翻转”的二值开关。用亮度过渡区与深色占比共同决定深色材质强度，避免少量黑字或单个暗角触发整框变色。估计器是材质控制信号，不声称精确检测“全部像素为黑色”。

| 项目 | 浅色/混合背景 | 大面积深色背景（候选方向） |
|---|---|---|
| 透射与模糊 | 保留当前背景颜色与模糊强度 | 保留真实暗背景，不铺不透明黑底 |
| 填色 | 维持当前极低强度白色 tint | 过渡到低强度中性暗色 tint |
| 内发光 | 保留非均匀方向与宽度 | 降低强度，白色改为柔和中性灰，避免灰白内圈 |
| 细边 | 保留浅色背景分离效果 | 减弱亮度/不透明度，仍可辨识 |
| 外扩散 | 保留零偏移、淡暗色扩散 | 适度减弱，避免重复加深形成厚边 |
| 圆角/主体尺寸 | 不变 | 不变 |

### 首轮视觉参数 v0.1（保留为标定起点，未冻结）

以下为保留的候选初值，用于独立静态渲染比较；本轮不凭生成图另造一组“已确认”数值。最终采用值以静态标定和用户确认记录为准；不是 Apple 标准，也不是已经得到用户视觉确认的效果。颜色按当前 shader 的**线性 RGB**口径填写，不能直接当作 sRGB 十六进制色值。

| 参数 | 当前浅色端点 | 建议深色端点 | 目的 |
|---|---:|---:|---|
| tintColor | [1, 1, 1] | [0, 0, 0] | 取消深背景上的白色洗亮 |
| tintOpacity | 0.008 | **0.012** | 极轻暗色混合，保留背景透射 |
| innerColor | [1, 1, 1] | **[0.30, 0.30, 0.30]** | 将内光改为中性灰 |
| innerStrength | 0.16 | **0.055** | 降低亮灰圈感 |
| innerWidth | 3 pt | **3 pt** | 保留已有深度宽度，不靠缩窄冒充降低亮度 |
| innerShade | 0.055 | **0.025** | 避免暗面过度压黑 |
| strokeColor（新增可控值） | 固定 [1, 1, 1] | **[0.45, 0.45, 0.45]** | 柔和中性细边 |
| strokeOpacity | 0.40 | **0.16** | 避免高亮白色轮廓 |
| strokeWidth | 0.55 pt | **0.55 pt** | 细边几何不变 |
| shadowColor | [0, 0, 0] | [0, 0, 0] | 保持零偏移暗色扩散 |
| shadowOpacity | 0.14 | **0.08** | 减弱深底上的重复加深 |
| shadowExtent | 6 pt | **6 pt** | 不因主题切换改变输出尺寸 |
| nonuniformity / lightDirection | 0.85 / [-0.6, -0.8] | 相同 | 保留非均匀光照方向 |
| sigma / saturation / brightness | 12 / 1.08 / 0 | 相同 | 不改变模糊和整体背景色彩处理 |

两端之间按一个整框系数连续插值。关闭外扩散/内光/细边的既有测试开关仍须优先，不能被自动深色端点重新开启。若需要读取旧 style 文件，新增 strokeColor 缺失时按白色端点处理。

### 首轮背景判定 v0.1

从主体区域均匀取得 8×8 个线性颜色样本。每个样本亮度 `Y = 0.2126R + 0.7152G + 0.0722B`；同时计算：

- `D`：`Y ≤ 0.10` 的采样比例，代表大面积深背景。
- `L`：`mean(min(Y, 0.20))`，将单个高亮样本的贡献限制在 0.20，避免黑底少量白字独自主导全框。
- 深色系数 `w = (1 - smoothstep(0.025, 0.10, L)) × smoothstep(0.75, 0.95, D)`，范围 0～1。

`w=0` 使用当前浅色端点，`w=1` 使用上表深色端点。纯黑/近黑区域趋于深色端点；白底黑字和明显明暗各半的区域不进入完整深色端点；黑底少量白字可保持主要深色外观。0.10、0.20 等是**线性亮度参数**，不是屏幕亮度百分比或操作系统主题状态。

这组参数属于待验证的工程提案，不作为自动验收标准。它估计背景分布，无法证明全部像素均为黑色；窄亮条、高频图案和边界移动必须用实际截图验证误判。统计端点测试可证明公式行为，不能替代真实材质视觉验收。

亮度必须统一在线性颜色空间计算，不能把 0～255 的 sRGB 数值直接套入线性阈值。上述初始亮度范围需通过黑、近黑、深灰、彩色暗背景与浅色背景的实际截图标定。图案背景和明显明暗各半的背景应保留中间/浅色方案，避免平均值单独主导误判。

## 性能实现建议

1. 复用已绑定到 GPU 的采样/模糊纹理，不新增屏幕捕获、不调用 CPU 全图亮度扫描，不改变现有精确像素去重。
2. 在已有 command buffer 中，合成前增加一次有界的小型 GPU 统计。首选固定 8×8 共 64 个分布采样，计算整框亮度与深色占比；纹理尺寸变大时采样数量不增长。统计直接写入一个很小的 GPU buffer，fragment 合成读取同一结果。
3. 不在每个 fragment 中重复执行整框统计，不将整帧读回 CPU，不增加 CPU 等待 GPU 统计结果的同步点，不增加独立 command buffer 提交。
4. 仅在原本需要渲染的新背景、几何或样式事件中更新。纯静态时不为了“检测主题”轮询、模糊或重绘；隐藏时延续原停采和零在途要求。
5. 初版用空间/亮度连续映射抑制阈值跳变，不新增持续动画定时器。首帧和恢复帧直接使用当前背景的正确材质，避免静态画面停在一半过渡状态。若真实测试仍出现闪烁，再讨论是否引入有明确停止条件的时间过渡，不能预先承诺 200ms 动画却省略其重绘成本。
6. 保持当前应用中间纹理上限与 GPU 在途上限；小型统计 buffer 单列计数，其实际分配不得以有效数据只有几个字节为由忽略。统计核的执行时间包含在原 GPU command buffer 耗时与预算中，不藏出测量口径。

小规模采样仍可能漏掉细小局部亮区，均匀采样也可能与规则图案混叠；需要高对比棋盘、窄亮条和移动亮区检查。对这种不确定场景，优先保持较保守的深色程度，不牺牲边界可见性。

## RSS 与内存增量：预期、限制与验证

**可能增加，当前未实测，不能承诺零增量或具体 MiB。** RSS 是进程当前驻留物理内存的口径，不等于全部分配量，也不等于 GPU 总内存。在 Apple Silicon 上 CPU/GPU 共享物理内存池，但各诊断计数覆盖范围不同，不能直接将 RSS、footprint 与 Metal 分配量相加作为总量。

| 来源 | 预期行为与控制 |
|---|---|
| 两组材质参数与插值 | 只有少量标量/向量；强弱和颜色变化本身不要求新增纹理。保持现有合成 pass，不为浅色/深色各渲染一张图再交叉混合。 |
| GPU 亮度统计 | 64 个样本在计算核内归约，不保存整幅亮度图或采样历史。结果拟用单个 float4（16 字节有效负载）描述亮度、占比、系数和保留位；一次创建、按当前单帧在途规则复用。实际 buffer 分配与对齐可能大于 16 字节，必须报告 allocatedSize。 |
| 新计算管线、shader 与驱动缓存 | 初始化和首次使用可能引入一次性分配/驻留增长；创建一次并复用，不能每帧或每次主题切换重建。具体开销必须实测，不能用 buffer 的 16 字节代替整个功能内存成本。 |
| 中间纹理和采集 | 自适应新增全尺寸中间纹理目标为 0，新增采集流为 0，保持现有模糊纹理复用。若将来发现必须新增，应先解释用途和实际尺寸成本，更新方案再实施。 |
| 诊断与预览 | 不把 PNG、历史帧、亮度日志累积在运行中应用；静态预览进程单独退出，不计入产品稳态样本。诊断沿用低频汇总，不为每次统计写单独文件。 |

RSS 的突然上涨可能来自首次访问页面、管线/驱动初始化或资源分配。一次跳升后持平不能单凭曲线认定泄漏；反复切换、缩放或恢复后持续抬高也不能用“只是缓存”直接解释。需对照资源数量、实际分配量和时间点定位。原 U02-06 的首次像素比较关联仍只是推断，不因本次方案升级获得根因证明。

### RSS 短测与完整功能验证分开

资源验证只保留一次预热后关闭/开启的短样本，报告 RSS 起止/峰值、CPU/GPU/FPS 与已有资源计数。不计算短窗长期斜率，不展开长时 RSS、多构建内存对照或 U02-06 排查；不宣称无泄漏。

功能验证按实际风险覆盖，不受 3～4 分钟或最多 3 张截图限制：

| 范围 | 必须核对的行为 |
|---|---|
| 正常启动 | 与开发二进制启动分别核对。正常 app 启动无权限时明确解释，拒绝可退出/重试，授权后恢复，必要时系统要求重启；零背景帧不能算材质通过。 |
| 透明毛玻璃 | 静态/移动彩色细节、文字和真实 Finder 内容确实透射且模糊；背景实时变化，定位正确，无实色替代板。 |
| 自适应 | 黑、近黑、深灰、暗色彩色细节、白、浅灰、明暗混合、窄亮条/棋盘；深浅往返，检查灰雾、突变及边界可见性。 |
| 几何与边缘 | 800×100 物理像素主体，以及放大/缩回；白/深背景观察细边、四角抗锯齿和零偏移外扩散；不可把柔和外阴影误作硬边模糊。 |
| 流光 | 实际右键入口开关、完整一圈及四角、渐隐线头、无硬头柔光、没有第二条内线；关闭清除、once 结束停止。 |
| 运动与生命周期 | 鼠标事件到 surface 的拖动、连续缩放期间相位冻结且映射仍显示，结束恢复；隐藏停调度、显示恢复，右键关闭退出。 |
| 异常路径 | 采集无权限/不可用、Metal 初始化失败不会静默伪装成功；状态及恢复入口可用。 |

保留必要截图/短动画或接触表与简短报告；不默认保存每帧完整日志或无上限证据。自动检查是运行证据，最终效果仍需直接查看，不能用计数替代视觉判断。真实锁屏与其他系统/屏幕环境仍沿用已确定的后续范围。

[U02-06](本机已知问题.md) 继续延期。只做代码开发和验证；打包/替换交付单独按用户要求执行。

## 可选流光：沿边高光与局部玻璃深度联动

用户接受首版单束流光方向，并明确要求流光区域映射到内发光/内阴影，避免孤立描边。本节及最新柔光概念图的视觉方向已获用户确认；具体数值强度、宽度及动画表现仍待标定和实现验证。

### 视觉与运动契约

- 单个冰蓝亮头配青紫渐隐尾迹，沿圆角矩形顺时针流动；一圈 6～8 秒作为初始候选，首轮以 7 秒预览。目标是炫酷而保持玻璃整体协调性，不用整圈同时变色或额外粒子替代局部流动。
- 运动按轮廓弧长推进，直边与圆角速度连续；尾迹跨越首尾接缝时连续。移动/缩放时冻结归一化相位，缩放几何更新后在新轮廓上重建弧长映射；不在屏幕绝对坐标固定亮头。不同尺寸保持一圈时长意味着物理移动速度随周长变化，本轮仅在一次缩放后的短观察中检查明显异常。
- 流光模块同时产生三种贡献：边缘亮头/尾迹、向主体内部无清晰边界地发散的柔光、可选的极轻弥散明暗过渡。三者共享同一相位与沿边衰减包络，内光亮度随亮头和尾迹衰减；内阴影不得独自提前、滞后或形成可见黑斑，也不能构成窄暗条。
- 光照映射沿局部内法线进入玻璃，圆角处随轮廓一起转向，不用固定向下偏移。内光从流光区域向内平滑扩散，靠近亮头的作用范围可稍宽、尾迹渐弱且渐窄；不能具有完整边缘、平行内描边、可辨认的截止线或完整内圈。中心区域没有跟随移动的大面积色雾；非流光区域保留基础材质。
- 首版不添加独立的内光追赶动画、不保留上一帧尾迹。柔和发散的明暗变化用于表达厚度，不改变背景纹理坐标、不造成真实折射位移。

| 可配置项 | 初步设置/边界 |
|---|---|
| 总开关 | `enabled=false` 为默认；开启后边缘、局部内光与局部内阴影一起生效 |
| 移动/缩放 | 高光和内部映射保持显示；冻结时间相位，停止流光专用唤醒，操作结束后继续 |
| 播放方式 | `loop` 持续循环；`once` 播放一圈后退出并清除全部增量 |
| 周期/方向 | 7 秒、顺时针为首轮候选；方向与周期独立可配 |
| 长度/颜色/强度 | 亮头、尾迹长度和颜色可配；精确数值需静态标定，不从概念图反推 |
| 内侧映射 | 独立 `innerGlowGain`、`innerShadeGain`、作用宽度；阴影弱于内光且无条带边界，全部由同一流光包络门控 |
| 外侧光晕 | 限制在现有透明留白内平滑归零；不增加窗口尺寸，不改写基础黑色外扩散 |
| 帧率 | 先以 30 FPS 预览；若拖尾/转角不流畅，评估轨迹与长度，再讨论提升帧率及代价 |

### 移动/缩放期间冻结规则（最新用户校正）

- 进入移动或缩放时保存相位，暂停流光时钟/专用唤醒；边缘高光、尾迹、柔光与弥散阴影使用同一冻结相位。用户总开关仍保持原值，不用自动关闭再开启模拟冻结。
- 冻结的是绕边流动，不是截图。窗口移动时流光附着窗口边缘；缩放时按最新圆角轮廓重新投影冻结相位与内部法线映射，不拉伸旧位图。归一化相位相同不保证在剧烈长宽比变化时仍落在同一条边；视觉要求是始终贴合新轮廓、映射一致。
- 基础玻璃继续按真实背景与几何事件更新；需要合成时带上冻结效果，不为保持冻结效果额外提交。背景自适应可因真实背景变化而更新，冻结相位不阻止正常材质调整。
- 操作结束且最终几何可用后，从保存相位继续推进，扣除暂停时长，不追赶经过的墙钟时间。`once` 模式也按有效播放时间计时；冻结期间不耗尽剩余动画。
- 优先使用已有拖动/缩放开始、结束及几何事件，覆盖用户拖动和程序触发的几何改变；不新增常驻轮询判断窗口是否运动。进入/退出冻结的状态版本须阻止迟到动画帧重新推进相位。
- 总开关关闭或窗口隐藏优先于冻结：关闭清除效果，隐藏停止调度；冻结不是强制显示流光。一次拖动和一次缩放的短测确认上述行为即可。

### 模块边界和关闭语义

新增独立 `BorderFlowStyle`、流光状态/时钟控制与着色函数，基础 `GlassStyle` 不持有被逐帧修改的流光效果值。模块单向读取圆角、主体尺寸、显示缩放、可见/可运行状态；如需适配背景，只读基础自适应系数，不写回判定阈值或基础端点。

合成顺序为基础玻璃结果叠加受限的局部明暗调制。“叠加”不是无限亮度相加：使用有界混合、正确的预乘 alpha 和边缘覆盖率，避免圆角毛边、亮头过曝和黑边。流光可以共用现有 render pass；逻辑独立不要求新增全尺寸图层、窗口或纹理。

关闭流光时，应取消其调度请求，清除局部增量；若最后一帧仍显示流光，允许一次基础恢复提交，之后静态背景不再重复提交。已在途帧必须经状态版本校验，避免关闭后迟到的流光帧重新显示。隐藏/锁屏时停止调度且不为清除流光强行显示窗口，恢复遵守当前可用背景与几何检查。

用户最新补充：外沿线条首端也改为渐变，不保留硬切端点。当前采用沿轮廓约 12 pt 总宽度的平滑过渡（首端前后各约 6 pt，小尾迹时按比例收窄）。

外沿线条与内部映射共享位置和速度，但不共用扩散宽度：内部柔光使用独立平滑的沿边头部包络，禁止将清晰线段头直接向内挤出成矩形切口。

三个贡献共用总开关；局部内光/阴影的单项开关仅用于诊断或调试，不增加框体内部业务 UI。基础关闭细边/内光的既有测试场景默认关闭流光，以保持历史基线；另行测试流光时明确开启项，不能误认为它修改了基础关闭标志。

流光自身不采集屏幕、不更改像素去重、不强制背景更新、不重新统计自适应亮度。动画帧复用当前有效背景和已计算的自适应结果；必须区分“合法复用未变化背景”与“采集失效后的陈旧背景”，不能为了动画跳过失效/恢复规则。

### 性能影响和独立验收

可见、开启循环流光且不处于移动/缩放冻结状态时，即使背景静止也需要持续呈现。原“静态不重复提交”继续约束流光关闭状态和基础采集/模糊工作；流光开启是明确的新动画场景，不宣称静态零功耗。基础材质动画与流光共用一个有上限的提交调度器和当前在途上限，避免两个时钟同时排队提交。

候选实现复用相位、沿边距离和衰减包络，在同次合成中计算内侧明暗；用连续光照衰减函数形成视觉上的模糊发散，作用区末端平滑归零，不用硬阈值截断。这里的“柔光类模糊”是光照分布要求，不强制新增图像高斯模糊。目标为无新增全尺寸纹理、模糊 pass 或历史帧；若解析衰减无法满足效果，须先对比新增 pass 的成本，再调整方案。仅动画相位变化时不得触发捕获、背景模糊或自适应统计。若继续采用全屏三角形，实际仍会重绘整个窗口，成本不等于“只画几个边缘像素”；本轮只在一次较大尺寸停留中观察明显卡顿，不展开最大尺寸 GPU/带宽专项测试。

RSS 可能因管线、调度与驱动缓存出现有界初始化增量；不承诺零增量。颜色和尾迹由公式生成，资源数量不得随圈数、开关次数增长。按上一节最小验证记录流光关闭/开启两段，不额外添加一套对照测试。

本节按上一节完整功能覆盖执行，资源专项维持短测。主要查看：局部柔光无第二条内线；一整圈无明显断裂；拖动/缩放时相位冻结且映射贴合；结束后继续；关闭后不残留；隐藏停调度。30 FPS 为首轮候选，若需 60 FPS，另行讨论成本；不在本轮自动升级帧率或延长测试。

### 参考与本轮预览

用户指定第一张流光图（exec-4106a0b6）更协调，保留其外沿亮头、颜色与炫酷感；后续弱化亮头的版本不作为视觉基准。用户补充：内部映射必须是柔光类模糊发散效果，不能形成第二条线。本节已据此替换原“内侧暗带”的表述。流光参考图与基础自适应参考图的 v0.2 分属不同序列，不混用版本号。

- [Magic UI Border Beam](https://magicui.design/docs/components/border-beam)：参考短亮头、渐变尾迹沿边移动的视觉方向。
- [Aceternity Moving Border](https://ui.aceternity.com/components/moving-border)：参考容器边缘连续运动。
- [Apple Metal 绘制方式](https://developer.apple.com/videos/play/wwdc2022/10114/)：持续动画与事件触发绘制的调度差别。网页案例不作为本项目原生性能证据。

本轮概念图片及审查见 [流光视觉 review v0.2](previews/流光视觉review-v0.2.md)。它用于讨论形态与联动，不能验证弧长速度、真实开关、背景复用、精确像素尺寸或性能；这些仍由后续公式渲染与运行测量确认。

## 变更范围与验收

预计产品范围为 GlassStyle、GlassRenderer、独立 BorderFlowStyle/流光控制与着色函数及必要的只读诊断字段；控制器仅增加受限的动画调度、状态失效和恢复接口。保留 AppKit 主线程边界、最新几何、采样新鲜度及 fallback 恢复规则，不修改 LocalCaptureService 的像素比较来实现材质亮度判断。系统 fallback 在无法取得真实纹理时继续由系统管理，不能用旧自适应状态假装有新背景。

保留 9mXYWr 作为已验收回退版本，不强制重新运行它做 A/B。功能与视觉按上述覆盖执行；历史资源长测和真实锁屏不自动重启。

仍保留主体尺寸、透明框体、柔光发散、独立开关及移动/缩放冻结的产品要求。此前最小验证仅覆盖所记载的开发进程短测，旧 U02-06 延期，其他环境和长期内存行为未验证。验证结束清理本次测试窗口与进程，证据只保留短报告及必要少量文件。

## 来源与边界

- [Apple — Analyzing memory usage](https://developer.apple.com/documentation/xcode/analyzing-memory-usage)：Metal 的实际资源分配可能大于请求量，需检查资源及实际分配，不以有效负载替代内存成本。
- [Apple — MTLResource.allocatedSize](https://developer.apple.com/documentation/metal/mtlresource/allocatedsize)：用于记录具体资源的分配字节数。以上资料支持测量方法，不证明本方案已经有确定的低内存开销。

- [Apple HIG — Materials](https://developer.apple.com/design/human-interface-guidelines/materials)：材质需要结合背景对比与视觉分离，深背景上的材质可以呈现更深的效果。用于设计方向，不代表本项目已经实现 Apple 私有材质。
- [Apple — MTLCommandEncoder](https://developer.apple.com/documentation/metal/mtlcommandencoder)：计算和渲染命令可编码到 command buffer。用于设计 GPU 内统计到合成的数据流，具体资源依赖仍须按实际编码顺序验证。
- [Apple — Synchronizing CPU and GPU work](https://developer.apple.com/documentation/metal/synchronizing-cpu-and-gpu-work)：CPU/GPU 资源访问与同步影响流水工作。用于避免新增逐帧 CPU 等待；本方案不据此扩大当前在途队列。

原已验收版本保留作为回退；此前曾更新应用 ZIP，但正常启动失败使验收重开。本轮按用户纠正仅开发源码，不继续打包或更新交付应用，也不启动 UP-03 内存排查。详见实现记录。
