# WP-09 最终验收报告（历史）

当前进展：用户已接受最大尺寸 RSS 已知问题，UP-02 当前构建完成剩余验收，本机版本为 CLOSED_WITH_KNOWN_ISSUE；当前构建见 [UP-02 报告](缩放CPU修复与最终验收-UP02.md)。以下是 WP-09 原 R5 的历史状态。

更新：2026-09-13。**BLOCKED_STALLED，尚未闭合。** R_PASS_5 仍有 R09-05：连续缩放 CPU 超过既定预算。验证批次为 INCOMPLETE，不能以旧构建的其他通过项拼成当前构建验收通过。

## 本次构建与验收范围

固定构建为 `RrZwDN`，本机 Apple M4 / macOS 26.5.2 / 内置 2x 屏。默认玻璃主体仍为 800×100 物理像素；外扩散透明边界使默认输出为 824×124 px。无系统装饰，仅框体 UI。本版范围遵循用户已确认的本机限定。

[F4 构建与签名](evidence/WP-09-build-F4.log) 完成，[自动检查](evidence/WP-09-checks-F4.log) 22/22 支持模块、6/6 生命周期通过。[ZIP 身份检查](evidence/WP-09-verification-20260913T104528Z/package.json) 确认 [历史失败基线压缩包](evidence/UP-02-baseline/GlassFrameLab-RrZwDN.zip) 内可执行文件与实测构建一致；这是待验收开发构建，打包一致性不是产品验收。

## R5 正式缩放结果

预热 30 秒、正式 300 秒，共 300 个 CPU/RSS 样本，P95 使用 nearest-rank；CPU 百分比按单核计。原始 [结果](evidence/WP-07-resize-20260913T104528Z/result.json)、[样本](evidence/WP-07-resize-20260913T104528Z/samples.csv) 和 [整批状态](evidence/WP-09-verification-20260913T104528Z/result.json) 保留。

| 指标 | 实测 | 预算 | 结论 |
|---|---:|---:|---|
| CPU 中位数 | 15.7364% | ≤15% | FAIL |
| 呈现 FPS 中位数 | 30 | 27～30 | PASS |
| 呈现间隔 P95 | 41.666625 ms | ≤50 ms | PASS |
| GPU P95 | 2.97308 ms | ≤12 ms | PASS |
| 采样到呈现延迟 P95 / 最大 | 57.83338 / 82.83325 ms | ≤100 / ≤250 ms | PASS |
| RSS 斜率 | 0.00279731 MiB/min | ≤1 | PASS |
| 末分钟减首分钟 RSS | 0.015625 MiB | ≤5 | PASS |

RSS 中位数 72.15625 MiB；本次没有完成同构建无窗口基线，不能据此声明增量内存预算通过。正式窗口内 8,998 个 GPU 样本、8,868 个正值呈现时间、130 个零呈现时间；零值不计入 FPS，也不单独说明实际不可见。几何提交 8,396 次，最终几何匹配，最终中间纹理 1 张、保留缓冲 1 个、在途 GPU 0。CPU/GPU 是能耗代理，未测精确瓦数。

## 已完成及尚缺的验证

[F4 限定回归索引](evidence/WP-09-regression-F4.json)：静态与动态兼容恢复通过，动态几何结构检查通过，样式切换保持 4 次合成、1 次模糊、1 次纹理分配。实际查看的 [6.50 秒圆角回归帧](evidence/WP-07-motion-20260913T104459Z/window-6.50.png) 边缘平滑；单帧不能替代完整最终视觉判断。

R5 在首个正式场景即发现 CPU 超限，因此后续默认/最大恢复、四场景完整性能、拖动、22 组视觉矩阵和真实应用背景未继续执行。本构建最终视觉判断未完成；前两次旧构建的无 VIS finding 结论只保留历史意义。当前结论见 [R5 审查记录](evidence/WP-09-verification-20260913T104528Z/review.json)。

WP-08 的真实锁屏冷启动/解锁、权限降级、全屏 Space 等本机实测保留原构建来源，见 [WP-08](workpacks/WP-08.md)；不把它们改写为本次构建的新测试。macOS 13、外接/跨屏、非 2x 和实际窄屏仍为后续 BLOCKED_VERIFICATION。

## 推理依据与下一步边界

[性能优化依据与论坛对照](性能优化依据与论坛对照-WP09.md) 汇总 Apple 文档、讲座、开发者论坛及作者实验，区分机制、个案和本机测量。F4 缓存透明承载内容、保留可见降级视图重绘并避免重复鼠标穿透赋值；正式 CPU 仍未通过，不能认定该优化已解决根因。

R3 / R4 / R5 正式缩放 CPU 分别为 15.03665% / 16.44% / 15.7364%。F4 短测 10.9405% 未在五分钟预算中复现，不能据此声称稳定节省。现有调用栈含布局、绘制、窗口提交和诊断写入，采样中的等待不等于忙 CPU，尚未定位唯一主因。

项目 [规约 §7.0](../开发规约与工作包计划.md) 明确：第 5 轮仍有 finding 时停止，不能自动新增 F5/R6。下一步建议由用户决定是否另开一个仅针对 R09-05 的工作包：先用固定负载区分忙 CPU 与等待，量化布局/绘制、窗口几何提交、诊断写入各自贡献，再选择有证据的最小修复，并以原预算重验。此建议未启动，未放宽预算。

用户随后要求先制定方案；已交付 [缩放 CPU 修复方案 v1.0](缩放CPU修复方案-v1.0.md)，包含新增文献/论坛依据、归因实验、条件性修复、原预算验收与回滚。状态为 planned_not_implemented，WP-09 仍为 BLOCKED_STALLED。
