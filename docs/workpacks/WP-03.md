# WP-03 — 局部屏幕采样服务

开始：2026-09-11，前置 WP-01/02 已关闭。

## Intent Freeze

- B03-01：只读检查录屏权限，未授权时明确报告 permission_required，不启动捕获，不高频重试。
- B03-02：只输出框体背后加模糊边距的局部区域，正确转换屏幕点与输出像素；cursor/audio 关闭，queueDepth=3。
- B03-03：排除本应用所有窗口，不出现自身递归；只有最新的一个视频缓冲在应用侧保留。
- B03-04：几何变化合并更新 sourceRect 和输出尺寸；异步更新串行；隐藏/关闭停止服务。
- B03-05：保存真实启动、帧尺寸与停止记录；权限不足时保持当前框体可用，真正系统材质 fallback 留待 WP-08。

允许修改：LocalCaptureService、局部采样几何、显式 --capture-probe 测试入口、生命周期接入和验证记录。范围外：Metal 模糊、视觉风格、产品内部内容、权限绕过、多显示器拼接。没有权限不得以合成帧作为真实采样通过。

WORKPACK_START: WP-03；验证：构建、采样矩形计算、权限分支、实际局部视频帧及自身排除（需要系统授权）。

## R_PASS_1（只读）

结果：FINDINGS。局部坐标检查 11/11 通过，但本包未关闭。

- R03-01：应用入口与 Timer 回调尚未适配主 actor 隔离，release 构建失败。
- R03-02：关闭已接入停止，但隐藏窗口尚未停止采样，与 B03-04 不符。
- R03-03：停止后已排队的视频回调仍可能重新保留缓冲；异步启动取消也需要清理迟到的成功结果。
- R03-04：真实权限/视频帧/自身排除尚未验证；README 的“不采集其他应用”描述需限定为默认模式。

## F_PASS_1

仅修复 R03-01～04，并补充权限检查和真实运行证据；没有权限时保留该未验证项，不扩大至 WP-04。

F1 修复映射：

| finding | intent | 修改及最小修复 | 验证 |
|---|---|---|---|
| R03-01 | B03-01/04/05 | main 的主线程入口/Timer 明确 actor 上下文；FramePixelSize 可跨闭包安全传值 | release 构建无警告、11/11 |
| R03-02 | B03-04 | main 在隐藏时停采，显式 windowWillClose 退出；辅助窗口/隐藏不触发产品退出 | 真实隐藏两次快照相同；CUA 右键关闭 |
| R03-03 | B03-03/04 | LocalCaptureService 关闭接收门并清空最新缓冲；启动取消清理迟到结果，更新错误检查版本 | 隐藏后 retained_buffers=0，帧计数停止 |
| R03-04 | B03-02/03/05 | CaptureProbe 临时自身色块、局部 PNG 与元数据；两个 capture 检查脚本；README 限定 opt-in | 局部视频、尺寸变化、自身排除真实证据 |

验证中发现：普通辅助窗口关闭及面板隐藏会触发 AppKit 普通窗口生命周期；F1 将退出绑定到实际框体关闭。截图为 Display P3，检查脚本按内嵌 ICC 转换 sRGB 后判断纯色，不放宽色块阈值。早期失败运行保留，未记为通过。

## R_PASS_2（只读）

- 构建：`docs/evidence/WP-03-build-F1d.log`，release 与严格签名检查通过，无编译警告。
- 生产纯逻辑：`docs/evidence/WP-03-checks-F1.log`，11/11。
- 局部视频/自身排除/隐藏：`docs/evidence/WP-03-capture-20260911T033425Z/result.json`。真实视频 472×122 px；自身色块截图纯色比例 1.0，采样中为 0.0；隐藏后两次计数均 64、缓冲均 0。查看了实际局部背景 PNG，未使用合成帧冒充视频。
- 实时尺寸：`docs/evidence/WP-03-geometry-20260911T033352Z/result.json`；sourceRect 和实际缓冲随放大、超限、恢复变化，关闭清空缓冲。
- 退出：`docs/evidence/WP-03-close-F1/termination.json`；CUA 右键菜单唯一项“关闭”，点击后退出，早于 120 秒定时器。
- B03-01～05：IMPLEMENTED_VERIFIED；权限拒绝分支为只读检查并报告状态，真实系统拒绝/撤销及 fallback 的兼容矩阵归 WP-08，不在本包声称通过。
- 暂不渲染采样帧，不要求本包完成玻璃视觉；没有按 CPU 绘制次数冒充 GPU 性能。

结果：NO_FINDINGS。WP-03 关闭，可开始 WP-04。
