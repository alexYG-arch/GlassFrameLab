# WP-07 — 动态几何与采样同步

状态：已关闭（R_PASS_4：NO_FINDINGS）。前置 WP-06 已在 R_PASS_3 关闭。

## Intent Freeze

- B07-01：外部尺寸输入继续 75 ms 去抖，显式测试入口提供有限时长尺寸过渡；普通变化保持当前中心，触边做最小位移，移动后不跳回旧中心。
- B07-02：动画可中断并接受最新目标，不产生布局循环；没有材质自身常驻动画，过渡结束停止几何时钟。
- B07-03：几何与样本映射匹配后才呈现；过渡期间仅采集当前与目标所需的有限局部区域，缓存可复用的有效背景，结束收回当前主体区域。连续移动按刷新预算合并配置，过期更新不覆盖最新状态。
- B07-04：真实截图/连续帧检查移动、尺寸变化、靠边与中断、样式 sigma/边距失效；记录采样范围、纹理分配和提交。静态结束后无重复工作，生命周期回归有效。
- B07-05：动态几何按已冻结的默认刷新、延迟、资源及内存预算验证；不扩大到内部布局或文件夹展开/收起。

范围：WindowGeometryController、LocalCaptureService 的映射更新、RealtimeGlassController 的几何失效、显式开发探针与验证脚本。沿用 WP-06 视觉预设，不重做 UI 内部内容。

WORKPACK_START: WP-07。

## R_PASS_1（只读）

初始构建 F7ovuI，支持模块与生命周期检查通过，无编译警告。`WP-07-motion-20260911T071829Z` 结构检查通过：47 次几何提交，最终主体 800×100 px，样式/几何一致，恢复 15 Hz、无持续重绘，纹理最多保留一张。截图中有透明帧，但探针截图时间从子进程创建计算，而应用几何日志从应用初始化计算，尚不能直接据文件名判断发生于哪个过渡阶段。

- R07-01 / B07-03：当异步配置提交过程中又收到相同目标，首次提交可能跳过 setMapping；后续合并分支仅在旧 output.mapping 等于新映射时开放接收，存在无法恢复接收的路径。
- R07-02 / B07-04：截图时间需绑定 ready.json 中应用实际起点，并记录实际拍摄时间。补齐可见性/坐标检查，不能把结构检查当作视觉通过。
- R07-03 / B07-05：动态几何完整预算和改动后的静态/生命周期回归仍需实测。

结果 FINDINGS。

## F_PASS_1

仅修复 R07-01～03，先验证映射合并与精确对齐的实际过渡帧，再依据真实结果处理当前 finding。

用户指出 `072507Z/window-6.50.png` 圆角毛边后，R07-02 定向排查确认实际 drawable 一直为 412×62 px，未随请求的 Retina 像素尺寸更新。已修正底层 CAMetalLayer 分辨率、原生窗口整点几何与 GPU 完成后的同步呈现，并为动态脚本加入实际纹理/图层/坐标检查。详细根因、阶段性失败证据及修复截图见 [圆角毛边排查](../圆角毛边排查-WP07.md)。

最终构建 9xnRJp：17/17 支持模块、6/6 生命周期检查通过；`WP-07-motion-20260911T074412Z` 中 71 帧实际纹理尺寸匹配、0 个坐标不匹配、22 张截图主体可见，结束后停绘。实际截图已检查，原阶梯毛边消除。此次尚未关闭 R07-03；真实分辨率下的完整性能需要重新测量，不能沿用 WP-06 历史预算结果。

`WP-06-runtime-20260911T074902Z` 是复用原脚本目录前缀的新构建回归：40 秒静态和 20 次显示/隐藏均通过，包含组合暂停、恢复新帧、最终停采/停绘和资源回收。它补齐短时回归，不替代 R07-03 的完整预算。

### 继续闭合时的定向验证

- 新增显式有限时长 `--motion-performance` / `--resize-performance` 探针，分别走真实拖动入口和可中断尺寸过渡；新增 `check_motion_performance.py`，短测仅作诊断，正式测量仍预热 30 秒后记录 300 秒。
- `WP-07-drag-20260911T075746Z`：短测中位 25.5 FPS，发现移动流程在 GPU 节流外重复等待；静态背景复用样本的年龄不能当作动态采样延迟。修复重复等待和连续移动中不必要的 envelope 收回，并改用动态背景验证真实延迟。
- `WP-07-drag-20260911T075951Z`：短测 29 FPS、帧间隔 P95 41.666584 ms、延迟 P95 66.16658 ms / max 74.50013 ms，通过短测门槛；不是五分钟通过证据。
- `WP-07-resize-20260911T080124Z`：短测 24 FPS、帧间隔 P95 74.999917 ms，FINDINGS。已针对同一过渡中待提交帧被下一 tick 过早废弃进行修复：保留在途帧，合并下一目标；真正取消/隐藏通过 epoch 失效全部旧几何。该修复仍待原生复测。
- `WP-07-resize-20260911T080419Z`：运行期间检测到真实 `login_window`，采样/提交均为 0；保留 `environment-interruption.json`，标为 `BLOCKED_VERIFICATION`，不是性能失败或通过。已请用户解锁，原生长测等待会话恢复。
- 同属 R07-01 的异步配置检查补齐：旧流 updateConfiguration 返回后，重新处理恢复期间排队的新流几何，避免 pendingWindow 留下却没有 worker。新增的过渡中隐藏/恢复探针将一并验证此路径。

当前仍为 F_PASS_1；未进行新的 NO_FINDINGS 判定，未启动 WP-08。WP-09 的正式效果开关 A/B 保留到该包；WP-07 使用三场景核心预算及拖动/尺寸变化长测。

当前构建 `gcvwC7` 构建/签名通过，无编译警告；17/17 支持模块与 6/6 生命周期检查通过（`WP-07-build-F1-pending-update.log` / `WP-07-checks-F1-pending-update.log`）。该构建的实际桌面验证尚未运行。恢复顺序：尺寸变化短测、包含隐藏恢复的 motion 检查、静态与 20 次循环、三场景核心长测、拖动及尺寸变化长测，然后进入 R_PASS_2；不把此前构建的短测转记为当前构建通过。

## R_PASS_2（只读）

用户解锁后，`WP-07-resize-20260911T091947Z` 当前构建短测为 27 FPS，帧间隔 P95 66.666417 ms，GPU P95 1.72117 ms，延迟 P95 66.16642 ms / max 74.49971 ms。帧率改善但帧间隔仍超限，暂不开展长测来重复已知失败。

- R07-04 / B07-01：连续中断缩放以已取整窗口中心作为下一次中心，误差累计；约 40 秒后默认主体从 `{100,739}` 移至 `{79,718}`，需要跨连续尺寸请求保持几何锚点，显式移动后重置。
- R07-05 / B07-02、03、05：新尺寸输入立即收回采样 envelope 并作废在途几何，然后再次去抖/准备，导致多余的帧间隙。应在接续过渡时保留有效采样区域，直到切换新目标时才失效旧几何。
- R07-06 / B07-01、03：普通无动画 request 直接 commit/stage，但新区域尚无样本时不能完成提交；该入口也需要受控采样准备，并有真实用例。
- R07-03 / B07-05：完整长测仍待上述已知失败修复后的固定构建。

结果 FINDINGS。此前圆角分辨率修复与短时恢复证据保留；它们不能关闭以上新增发现。

## F_PASS_2

仅处理 R07-03～06：稳定中心锚点、接续过渡的采样/几何切换、普通尺寸请求准备与对应原生回归，随后按冻结预算长测。

尺寸请求现在跨接续过渡保留未取整中心锚点；每个中间帧围绕同一锚点计算，显式移动/显示器变更后重置，触边时更新到受约束中心。普通无动画请求也先准备有效采样，再一次提交最终尺寸；motion 探针增加 1200×180 px 普通请求及恢复默认尺寸断言。

过渡切换在去抖期间保留有效采样 envelope，不立即收回再扩张。新的过渡开始时才通过 epoch 取消旧几何；同一过渡内已进入 GPU 的帧可以完成，后续目标合并排队。准备期间隐藏不再引发应用退出，显式恢复后的最新请求重新准备。

`WP-07-resize-20260911T092412Z` 显示最终中心已回原位，但中间帧仍有额外取整偏差；已进一步固定中间帧锚点。`093134Z` 中中心检查通过，应用取消/坐标拒绝均为 0，但 transaction 呈现模式仍出现 146 个零呈现时间记录，帧间隔超限；保留该结果。

针对实际呈现模式作单项对照：恢复 CAMetalLayer 默认异步 Metal 呈现，仍在 GPU 完成后以对应目标更新窗口和 drawable，并提交/flush AppKit 几何事务。`WP-07-resize-20260911T093429Z` 构建 `o3MDY2`：29 FPS、帧间隔 P95 41.666583 ms、GPU P95 2.60279 ms、延迟 P95 57.83325 ms / max 66.16675 ms，中心稳定检查通过，实际截图圆角平滑。少量零呈现时间记录仍保留并排除于呈现 FPS，未更改指标口径或预算。

固定 `o3MDY2` 开始 `verify_wp07.py` 串行验证；它只自动判定工程检查，最终仍需新的 R_PASS 复核，不能将脚本运行中或结构通过等同于 workpack 关闭。`092800Z` 的另一次 login_window 中断已记录，后续只读桌面检查确认恢复后才继续；本轮防休眠仅为有时限临时断言，不改变系统设置。

首次串行运行 `WP-07-verification-20260911T093605Z` 的 motion、样式、默认和最大尺寸回收检查通过。继续检查原始记录发现：静态恢复首帧在视图透明时提交，随后显示，所有首次 drawable 的 presentedTime 可为 0；提交数不能单独证明逐次恢复可见。为补齐证据，在核心静态测量约 90 秒时有序中断，原运行保持 INCOMPLETE，不计五分钟结果。

构建 `6LthON` 将确认有效帧后的视图显示和几何事务提交放在 drawable.present 前；静态首帧仍可能没有有效 presentedTime，因此不将它强行转换为正值，也不为统计补绘重复帧。恢复检查改为 20 次逐次原生窗口截图，验证玻璃主体实际可见；仍记录新捕获/提交、隐藏释放和原始零时间戳。动态 FPS/延迟保持只用正值 presentedTime。重新运行整套串行验证，当前仍未作关闭结论。

## R_PASS_3（只读）

固定构建 `6LthON` 的串行验证 `WP-07-verification-20260911T094632Z` 完成了动态截图、样式缓存、默认/最大尺寸各 20 次实际恢复截图、核心三场景五分钟预算和连续移动五分钟预算。以上检查通过；缩放长测返回 FINDINGS，整套运行保留 INCOMPLETE。

R07-04 的中心累计偏移已消除：缩放长测 8142 次几何提交，中心最大偏差 0.5 pt，最终回到默认主体。R07-05 的重复采样准备及应用主动取消问题未复发，cancelled/mismatched geometry 均为 0；R07-06 的普通请求已在原生 motion 检查通过。

- R07-07 / B07-03、05：`WP-07-resize-20260911T101338Z` 中位 28 FPS，但帧间隔 P95 66.6665 ms 超过 50 ms。8722 条正式 GPU 记录中有 609 个零呈现时间戳；应用没有取消或坐标拒绝。GPU P95 5.54092 ms、CPU 14.0469%、延迟 P95 57.83333 ms / max 124.49971 ms 和内存均通过。零记录主要集中在第 90～150 秒，短测未能代表长测；需检查窗口几何事务与 drawable 排队的先后时序，不通过删零记录或提高阈值关闭。
- R07-03 / B07-05：核心和移动预算已补齐，缩放预算仍因上述 finding 开放。

结果 FINDINGS。未开始 WP-08。

## F_PASS_3

仅处理 R07-07 和对应 R07-03：在 GPU 完成且目标几何有效后，将 drawable 排队与窗口几何放在同一次主线程事务提交之前，避免先 flush 新几何再排队新图像的可见空隙；保留默认异步 Metal 呈现。此为待验证的定向修复假设，不预先宣称根因或性能通过。先验证缩放，再按实际影响补齐回归。

`WP-07-resize-20260911T102059Z` / 构建 `1XCWlX` 的正式 300 秒采样通过：29 FPS，帧间隔 P95 41.666584 ms，GPU P95 2.9025 ms，采样到呈现延迟 P95 57.83329 ms / max 91.16667 ms，CPU 单核中位 14.06285%，RSS 斜率 −0.784 MiB/分钟、末减首 −3.547 MiB。8166 次几何提交的中心/结束状态检查通过；8715 条正式 GPU 记录中 71 个零时间戳未计入呈现。该结果支持呈现顺序修复有效，但不宣称零值完全消失。

构建/签名与 17/17 支持检查、6/6 生命周期检查通过（`WP-07-build-F3-order.log` / `WP-07-checks-F3-order.log`）。`verify_wp07.py --completed-resize` 只允许复用同一完整应用二进制路径、完整采样且全门槛通过的缩放结果，其余受影响场景重新验证；旧构建数据不自动升级为新构建通过。

补齐运行 `WP-07-verification-20260911T102715Z` 使用同一 `1XCWlX` 构建：原生 motion 的 62 帧纹理/坐标及 22 张主体可见性检查通过，实际放大和中断截图已复核；样式缓存、默认及最大尺寸各 20 次恢复通过。新构建静态五分钟 CPU 中位 0.30955%，正式段无 GPU 提交，内存稳定。动态、最大尺寸及移动预算仍在串行验证，尚未进入 R_PASS_4。


## R_PASS_4（只读）

复核固定构建 `1XCWlX`，未修改产品代码。逐项对照 Intent Freeze、生产几何/采样/呈现路径、原生截图和完整预算。

- B07-01：75 ms 去抖、普通请求和可中断动画入口均有覆盖；连续缩放 8166 次提交，中心最大偏差 0.5 pt，不累计漂移；移动后的锚点、靠边约束及最终 800×100 px 均通过。R07-04、06 关闭。
- B07-02：动画中断与准备期间隐藏恢复通过，末态无几何时钟；连续移动结束 150 ms 后收回 envelope，静态末段停止重复 GPU 工作。R07-05 关闭。
- B07-03：几何与采样映射共同校验；当前原生检查 62 帧纹理尺寸正确、0 个坐标不匹配，实际输出保持 Retina 像素；旧流配置返回后能排空新流 pending intent。新 drawable 在几何事务 flush 前排队，缩放完整测量达到 29 FPS、帧间隔 P95 41.666584 ms，R07-01、07 关闭。保留原始零时间戳，不将它们伪造为呈现。
- B07-04：22 张主体可见截图及移动/缩放/最大尺寸/恢复代表截图已查看；圆角毛边未复发，背景连续，样式失效及缓存符合预期。默认、最大尺寸各 20 次恢复逐次截图通过。R07-02 关闭。
- B07-05：默认静态、默认动态、最大尺寸、移动、缩放均完成 30 秒预热后 300 秒采样；同构建基线和回收对照齐全。汇总 30 项预算检查全部通过，R07-03 关闭。默认动态 P95 50 ms 恰好达到上限；CPU/GPU 为代理，未宣称瓦数或跨设备通过。

证据：[串行运行](../evidence/WP-07-verification-20260911T102715Z/result.json)、[完整预算汇总](../evidence/WP-07-verification-20260911T102715Z/budget-consolidated.json)、[性能报告](../动态几何性能报告-WP07.md)。构建/签名、17/17 支持模块、6/6 生命周期检查通过。未发现当前包范围内开放问题。

WORKPACK_CLOSE
id: WP-07
status: NO_FINDINGS
validation: PASS
visual_check: PASS
performance_check: PASS
open_findings: NONE

WP-08 的降级与环境兼容、WP-09 的正式 A/B 和最终验收尚未完成；本关闭不覆盖它们。
