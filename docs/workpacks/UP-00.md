# UP-00 — 恢复时序限定修复

状态：已关闭（NO_FINDINGS）。用户于本轮明确授权“执行升级”，本包按升级计划重新冻结范围；保留 WP-05 第五轮历史，不执行其第六轮。

## Intent Freeze

- U00-01：启动未完成时隐藏→恢复，最终按最新可见意图运行，不丢失恢复请求。
- U00-02：停止未完成时恢复，不被旧停止结果覆盖；暂停原因组合和终止停采优先级保持有效。
- U00-03：隐藏立即禁止接收/呈现新帧，清理异步启动和在途工作；保留隐藏预算和有界资源。
- U00-04：通过可控异步时序测试及真实静态/20 次恢复验证。真实锁屏冷启动缺失仍明确保留，等待本轮环境验证安排答复。

范围：实时协调器、采样失效入口、生产协调逻辑的受控异步验证；不修改视觉、尺寸或预算。

WORKPACK_START: UP-00。

用户已明确选择“保留到 WP-08，先完成升级”：真实锁屏冷启动与 macOS 13、外接屏均作为 WP-08 未验证环境项目；本包的验收使用生产协调器受控交错及当前真实窗口测试，不宣称实际锁屏通过。

## 实现与映射

U00-01/02：新增生产 RunIntentCoordinator，串行 start/stop，保留最新意图与必须清理旧流的标记；RealtimeGlassController 使用它收敛状态。U00-03：LocalCaptureService.suspendFramesAndPendingStart 同步关闭帧接收并作废旧启动；停止等待 GPU 清理。U00-04：LifecycleChecks 直接调用生产协调器，通过受控 continuation 构造启动中、停止中、同一事件轮次以及最终隐藏的交错。

## R_PASS_1（只读）

- `UP-00-checks-final.log`：原辅助逻辑 13/13，新增生命周期 6/6；`UP-00-build-final.log` 构建、严格签名通过。
- 最终构建 `/private/tmp/glassframe-build.09RN8Q/GlassFrameLab.app`，真实证据 `WP-05-runtime-20260911T051758Z`：静态稳定后提交不再增加，15 Hz 上限；组合暂停正确，20 次恢复均新增渲染，最终采样缓冲及 GPU 在途归零，内存回收满足原循环门槛。
- `WP-05-runtime-20260911T051518Z` 属于先前构建，保留但不作为最终构建证据。
- 源码检查：暂停立即失效帧；新启动等待旧操作收敛，同轮隐藏/显示也必须先清理，不仅比较最后的布尔值。无新增高频定时器或中间纹理。
- U00-01～04 IMPLEMENTED_VERIFIED（真实锁屏环境项按用户明确指令留到 WP-08）。本包没有开放 finding，不修改旧 WP-05 第五轮记录。

结果：NO_FINDINGS。UP-00 关闭，允许进入 UP-01。
