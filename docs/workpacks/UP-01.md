# UP-01 — 光晕透明边距与玻璃坐标

状态：已关闭（NO_FINDINGS）；前置 UP-00 已关闭。

## Intent Freeze

- U01-01：默认玻璃主体 800×100 显示 px 不变；每侧初始 6 pt 透明光晕边距计入承载窗总尺寸，不新增可见底板。
- U01-02：主体、panel、drawable、采样坐标显式分离，主体中心和原屏幕约束保持有效；最大主体扣除边距。
- U01-03：背景只采玻璃主体加原模糊边距；默认当前 2x 非贴边采样仍为 472×122 px，无拉伸或偏移。
- U01-04：右键关闭和主体拖动仍有效，透明边距不增加控件；旧诊断明确区分主体和总尺寸。
- U01-05：生产几何检查、真实尺寸/采样与单帧截图验证，纹理数量不因透明留白增加。

范围：几何与窗体承载、采样映射、renderer 坐标、相关诊断/检查。视觉光晕在 WP-06 绘制；不调整生命周期或性能预算。

WORKPACK_START: UP-01。

## 当前验证进度

- `UP-01-checks-final.log`：14 项辅助逻辑及 6 项生命周期检查通过；`UP-01-build-final.log`：构建及严格签名通过，当前应用位于 `/private/tmp/glassframe-build.Xs0Zn7/GlassFrameLab.app`。
- `WP-02-geometry-20260911T052344Z`：原生默认主体 800×100 px，总窗口 824×124 px；放大、上限夹取、恢复和重复输入抑制通过。其构建为加入鼠标穿透监听之前的几何版本，不代替最终交互验证。
- `WP-03-geometry-20260911T052352Z` 在系统锁屏期间启动，状态 desktop_unavailable、零采样；本轮采样几何检查中断，不能记为通过。
- 公开 API 随后观察到 frontmost=com.apple.loginwindow。最终构建 `UP-01-locked-start` 实时模式冷启动记录 login_window blocker，捕获/提交/纹理均为 0，正常退出。它验证了当前系统真实锁屏后的拒绝启动行为，不证明解锁恢复或其他系统兼容。
- 新增鼠标移动事件监听按玻璃圆角区域切换穿透，不使用周期轮询、不记录指针轨迹；最终交互测试尚待解锁。

当前为验证阶段 BLOCKED_VERIFICATION（等待桌面解锁），尚未进行本包最终 R_PASS，也未进入 WP-06。

## R_PASS_1（解锁后只读）

`WP-02-geometry-20260911T055341Z`、`WP-03-geometry-20260911T055348Z`、`WP-04-preview-20260911T055355Z` 分别通过实际尺寸、采样尺寸变化、单帧纹理/透明四角检查。已查看前景截图，主体未拉伸，前景清晰。

`UP-01-interaction` 记录背景夹具收到一次点击，右键菜单关闭让应用在 101 秒退出（早于 140 秒自动退出）。拖动未改变窗口位置；不能记作交互全部通过。

Finding U01-F01 / U01-04：穿透判定使用 NSEvent.mouseLocation，而不是当前收到的事件坐标。电脑操作工具的事件位置与即时全局指针读数可能不同；穿透可能在拖动时错误保持。应依据事件的全局坐标更新，并复核主体拖动与留白点击。

结果 FINDINGS。

## F_PASS_1

仅修复 U01-F01：事件监听使用事件位置；无事件的初始化/窗口几何更新仍读取当前指针。补测交互，不更改视觉或预算。

## R_PASS_2（只读）

F1 构建/签名通过。`UP-01-interaction-F1` 中主体横向拖动后实际 CGWindow bounds 仍为 x94/y187，未产生新 geometry commit；右键菜单仍能打开。事件坐标修复不足以证明非激活窗首击拖动有效，U01-F01 仍开放。

## F_PASS_2

仅修复 U01-F01：主体视图显式接受首击，并通过公开 NSWindow.performDrag(with:) 处理拖动，避免依赖透明 Metal 视图上的隐式背景拖动。

## R_PASS_3（只读）

F2 构建通过；`UP-01-interaction-F2` 的坐标右键能打开菜单，说明主体坐标可被投递，但 drag 仍没有 geometry commit。显式系统 performDrag 在当前非激活透明窗/自动化事件组合下未证实有效，U01-F01 仍开放。

## F_PASS_3

仅修复 U01-F01：以主体 mouseDown/mouseDragged/mouseUp 保存拖动锚点并更新窗口原点，交给既有几何控制器约束；拖动期间固定接收鼠标，结束后恢复透明边距穿透。不依赖系统隐式拖动跟踪。

## R_PASS_4（只读）

F3 的 `UP-01-interaction-F3` 产生实际 x94→134 的 40 pt 拖动，主体 800×100 px 不变，sourceRect 同步 x64→104，仍只一张中间纹理；87 秒通过右键退出，早于 140 秒定时结束。U01-F01 的拖动部分通过。

Finding U01-F02 / U01-04：电脑操作坐标点击不总是先产生 mouseMoved，新的 `UP-01-padding-F3` 尚无背景点击记录；当前监听仅处理移动/拖动，不处理按钮事件，穿透状态可能滞后。需要对按钮事件同步更新穿透并验证，不把旧构建记录当成本构建通过。

## F_PASS_4

仅修复 U01-F02：将按下/释放纳入同一事件驱动穿透判定；拖动期间仍保留接收。补充当前 ignore 状态诊断，仅记录布尔值，不记录用户指针轨迹。

## R_PASS_5（最终只读）

`UP-01-build-F4.log` 构建/签名通过，`UP-01-checks-F4.log` 为 14+6 项通过。

最终 `UP-01-interaction-F4`：玻璃显示且 mouse_ignored=true 时，在底层夹具的屏幕留白位置 x96/y218 点击，夹具记录 FIXTURE_CLICK (96,100)。随后主体实际从 x94/y733 移到 x134/y713，尺寸 412×62 pt 不变；主体 800×100 px；sourceRect 相应变为 x104/y177、472×122 px；一张中间纹理。82 秒通过右键关闭，早于 150 秒自动退出。工具对固定 app 的直接点击不能独立证明跨 app 路由，所以使用底层窗口接收记录与玻璃 ignoresMouseEvents 状态共同核对，不把早期未收到的点击当作通过。

几何/采样/单帧输出逻辑在 F1～F4 未改变，沿用前述实际截图与尺寸证据；最终构建再次通过生产几何检查和实时尺寸/源区域记录。事件监听无定时轮询，保留拖动期间接收和释放后穿透。测试辅助指针进程已退出。

U01-01～05 IMPLEMENTED_VERIFIED，无开放 finding。结果 NO_FINDINGS，UP-01 关闭。
