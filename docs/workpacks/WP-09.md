# WP-09 — 最终视觉与性能验收

当前产品状态：UP-02 已修复缩放 CPU，并在用户接受 U02-06 后完成带已知问题本机验收，见 [报告](../缩放CPU修复与最终验收-UP02.md)。下文保留 WP-09 原 R5 历史失败及轮次。

状态：BLOCKED_STALLED。R_PASS_5 仍有 R09-05（缩放 CPU 15.7364% >15%），四次 F 已用尽；最终视觉判断已完成 2 次，均为旧构建历史结论，当前构建完整视觉未完成。前置 WP-08 本机范围在 R_PASS_3 关闭。

## Intent Freeze

- B09-01：当前构建真实截图覆盖 V-01～12 的本机适用部分、六种背景、默认和较大尺寸、逐层开关、清晰前景、实际普通应用窗口、动态几何与恢复；最多 3 次最终视觉判断。
- B09-02：同一固定构建完成预热 30 秒、正式 300 秒的静态、动态、最大尺寸、效果全关对照及拖动/缩放；复用本构建已通过的最大尺寸 20 次恢复，本轮补默认 20 次恢复。所有既定预算不变。
- B09-03：资源与隐藏停采有证据，报告 CPU/GPU 代理和 A/B 噪声边界，不宣称精确瓦数。
- B09-04：本版仅当前 Apple M4 / macOS 26.5.2 / 内置 2x；macOS 13、外接/跨屏、非 2x/实际窄屏仍是后续未验证，不转写 PASS 或不适用。
- B09-05：无当前范围开放 finding；交付构建、运行说明、证据索引和状态一致。不新增产品能力。

R1 历史固定产品构建 `/private/tmp/glassframe-build.aDPLug/GlassFrameLab.app`；构建/签名及 18/18 支持模块、6/6 生命周期检查来自 WP-08 F1，产品代码尚未再变。最大尺寸 20 次恢复复用 `WP-06-runtime-20260911T112704Z/cycles`。

WORKPACK_START: WP-09。R_PASS_1 只读审查产品实现并获取当前构建证据；验证脚本和报告为本包交付。尚未给出最终视觉或预算结论。

## R_PASS_1 进行中记录

- 最终背景矩阵 `WP-06-visual-20260911T114908Z` 的 22 组原生截图已收集；新增当前实时场景 `WP-09-context-20260911T115052Z` 覆盖白色、多色和实际 Finder 窗口。已查看背景/分层对照、前景、实时截图，以及当前构建 motion 中 3.45/6.50/11.50 秒的原始图像；暂未发现视觉问题，整组最终判断将在正式动态场景截图复核后结束，仍计第 1 次。
- R09-01 / B09-05 / P2（文档）：README 下部仍写 WP-08 执行中、WP-09 未开始，且把真实锁屏列作未验证，与本次实测及用户范围变更不一致；冻结预算文档的历史环境缺口也需增加当前报告指引，保留原数值和历史时点。R1 收齐结果后进入 F1，只统一当前状态与最终交付入口；保留历史失败/旧构建报告。
- 交付 ZIP 中的可执行文件与当前实际测试的 aDPLug 完全一致，见 `docs/evidence/WP-09-package.json`。该检查仅验证打包一致性，不替代运行与视觉结果。

### R_PASS_1 结论

最终视觉判断第 1 次：已检查本构建背景矩阵、实际 Finder、前景、默认/最大恢复、锁屏恢复、既有动态几何和本轮正式静态/动态/最大/拖动截图，没有 VIS finding。性能独立判定：四场景与隐藏预算通过，但 `WP-07-drag-20260911T121549Z` 的最大采样到呈现延迟为 345.33238 ms，超过 250 ms；整批 `WP-09-verification-20260911T114908Z` 为 INCOMPLETE，尚未运行正式缩放。

- R09-02 / B09-05 / P2：新锁屏验证脚本依赖先前生成的 WindowOverlapDiagnostic，没有自包含的构建入口，应从现有只读 Swift 源码生成本次诊断工具。
- R09-03 / B09-02 / P1：拖动记录中 4533/4534 序列出现 299.50、312.00、345.33 ms 延迟；GPU 时间分别约 1.22、5.62、0.03 ms，主要延迟在 GPU 执行之外，现有记录无法进一步区分捕获交付、主线程或合成器等待。代码允许几何变更复用过旧采样，提交几何前没有时效检查。应在提交 GPU 前及呈现/几何提交前拒绝过期几何采样，保留最新待提交几何，等待新有效样本；静态无变化不能因此循环重绘。须验证延迟完成后的恢复与现有预算，不把被拒绝帧记为呈现，也不能只修改统计口径。

R1 结果：FINDINGS，开放 R09-01～03。原始失败证据保留，不放宽预算。

## F_PASS_1

范围仅 R09-01～03：当前文档入口与锁屏诊断工具可复现性；几何采样时效检查及一次性有效帧通知、有限延迟完成测试。GlassStyle 与 Metal shader 不变。产品构建变更后，正式预算必须使用新固定构建；旧结果保留为诊断与对照，不能拼接成新构建通过。


F1 验证记录：RSUbup 构建/签名、19/19 支持模块、6/6 生命周期检查通过。首次新增静态兼容测试 `WP-08-compat-20260911T122822Z` 在 recovered_stopped 的旧断言失败：该时刻原生输入只有 idle、没有新像素缓冲，继续显示 fallback 符合“仅有效新样本恢复”的原约束；后续真实重启得到新 complete 帧后恢复，最终 stale_geometry_frames=1 且目标尺寸提交成功。修正静态测试的前提：无新缓冲时必须保持 fallback，获得有效样本的流重启、重绑定和最终延迟恢复仍强制要求实际材质可见。动态测试原有恢复要求不变；未修改产品来伪造静态像素。该首次失败原始结果保留，正在重新验证。

F1 限定回归已完成：静态 `WP-08-compat-20260911T123106Z` 与动态 `WP-08-compat-20260911T123205Z` 均通过；每组 final 记录 stale_geometry_frames=1、capture_start_attempts=4、目标窗口与实际渲染几何一致。350 ms 为明确的测试注入，产品默认没有该等待。`WP-07-motion-20260911T123304Z` 结构检查通过，`WP-06-style-updates-20260911T123319Z` 仍为四次 GPU 提交、一次模糊、一次纹理分配。R09-01 当前入口已更新，R09-02 改为每次由 Swift 源码构建只读工具。R09-03 的正式预算复核尚待 R2，不因短测或注入通过而关闭。

## R_PASS_2（只读，正式复核中）

固定产品构建 RSUbup，背景夹具 DvoHjb。先测先前失败的连续拖动，再测连续缩放；其后重取最终视觉矩阵（模型视觉判断第 2 次）、默认与最大 20 次恢复、静态/动态/最大/光效关闭的同构建正式五分钟测量。所有数值保持原预算。尚未给出 NO_FINDINGS。

### R_PASS_2 结论

`WP-09-verification-20260911T123636Z` 为 INCOMPLETE。拖动、缩放、默认与最大 20 次恢复通过；最终视觉判断第 2 次为 NO_VISUAL_FINDINGS。拖动最大延迟 157.83313 ms、缩放最大延迟 91.16646 ms，R09-03 本轮正式复核通过。四场景中静态、动态和最大尺寸各有 300 个正式样本，但效果关闭仅 89 个样本，不能判定整批预算通过。上次中断后的原始文件均保留。

- R09-04 / B09-02 / P1：默认动态 `WP-06-performance-20260911T125403Z/dynamic` 的呈现间隔 P95 为 54.166583 ms，超过 50 ms；FPS 中位数 27、CPU 5.45815%、GPU P95 3.58508 ms。当前提交节奏从每次实际执行时间重新计算，会把调度迟到累积到后续期限。这是可定位的节奏缺陷；现有证据不证明它是全部抖动的唯一来源。需保持稳定期限、长暂停后不追赶补帧，并保留按需渲染与单帧在途，正式复核不得放宽原预算。

R2 结果：FINDINGS，开放 R09-04。R09-01～03 的限定复核已通过。

## F_PASS_2

只修正 R09-04：提交期限保持固定相位，迟到不足一周期时不把迟到累积至后续帧，错过整周期后从当前时间恢复；15/30 fps 切换重算期限，隐藏时重置。仍由有效内容变化触发单次唤醒，没有新增轮询或重复静态绘制。加入调度迟到、长静止恢复和采集帧率切换的支持模块检查。待新构建和正式预算验证。

F2 构建 JrJrwq 已完成构建/签名、22/22 支持模块和 6/6 生命周期检查（`WP-09-build-F2.log`、`WP-09-checks-F2.log`）；ZIP 可执行文件逐字节一致，见 `WP-09-package-F2.json`，原生验收仍待完成。临时背景夹具已由现有源码重建。2026-09-13 的原生静态回归 `WP-08-compat-20260913T093643Z` 在 initial 检测到 `desktop_blockers=[login_window]`，自动停止，未采集锁屏画面；这是环境阻塞，不计产品 finding，也不计最终视觉判断。已请用户解锁。测试顺序改为优先默认动态场景，完成五分钟后立即按原预算判断；超限即停止后续场景，避免已知失败后的无效整批测量。

F2 限定回归完成：静态 `WP-08-compat-20260913T093820Z`、动态 `WP-08-compat-20260913T093928Z` 均 PASS；`WP-07-motion-20260913T094037Z` 结构检查通过；`WP-06-style-updates-20260913T094052Z` 仍为四次提交、一次模糊、一次纹理分配。用户已解锁。

## R_PASS_3（只读，正式复核中）

固定产品构建 JrJrwq，使用当前源码构建的背景夹具。先完成默认/最大 20 次恢复与默认动态五分钟预算，再收齐其余正式场景和最终视觉判断第 3 次。保留原数值、失败历史和本机验收范围；未完成前不作 NO_FINDINGS。

### R_PASS_3 结论

JrJrwq 的默认/最大 20 次恢复、四场景及隐藏预算、正式拖动通过。默认动态为 29 FPS / P95 41.6665 ms / 最大延迟 57.83317 ms，R09-04 的节奏修正正式复核通过。拖动为 30 FPS / P95 41.666583 ms / 最大延迟 207.83296 ms，延迟问题未复发。

- R09-05 / B09-02 / P1：`WP-07-resize-20260913T101343Z` 的 300 个正式 CPU 样本中位数 15.03665%，略超 15%；不能通过四舍五入或提高预算认定通过。其余缩放指标通过：30 FPS、间隔 P95 41.6665 ms、GPU P95 2.26429 ms、最大延迟 132.83304 ms、内存斜率 −0.02210 MiB/分钟。需针对缩放时宿主/布局 CPU 开销定位并修正，不减小测试尺寸、不降低视觉或帧率门槛。

整批 `WP-09-verification-20260913T094131Z` 为 INCOMPLETE。最终 22 场景矩阵和实际上下文未执行；已查看的本构建几何/恢复/四场景图片保留为诊断，没有完成第 3 次最终视觉判断，不能把这些片段拼接成下一构建的视觉通过。下一固定构建仍须取得完整当前证据，最终判断上限不变。

R3 结果：FINDINGS，开放 R09-05。正在对本项目缩放进程作有限调用栈采样；附带短测只作诊断，不能替代正式预算。

## F_PASS_3

仅修正 R09-05 的缩放宿主开销。有限调用栈 `WP-07-resize-20260913T102041Z/cpu-sample.txt` 捕获了提交几何时的立即 display 路径、隐藏 fallback 的 `FrameSurface.layout → NSVisualEffectView.setFrameSize` 更新，以及原有诊断写入；采样不是精确 CPU 成本分摊，也不是正式预算。选择只减少两处实际 UI 冗余：成功 Metal 材质状态下不强制立即 AppKit display，保留 carrier/fallback 的即时显示；fallback 隐藏时不更新其几何/边框，重新显示时由既有 needsLayout 更新。Metal 呈现与几何顺序、光效参数、帧率、记录数量和诊断写入均不变。

新构建 Sdh5qG 已构建/签名，正在限定回归。后续正式验收先运行缩放，已知预算问题未解决时不继续无关场景；所有正式结果仍须同一固定构建。

F3 构建/签名、22/22 支持模块和 6/6 生命周期检查通过（`WP-09-build-F3.log`、`WP-09-checks-F3.log`）。静态原生回归 `WP-08-compat-20260913T102337Z` 在 initial 检测到 login_window，停止且没有采集锁屏图像；待用户解锁，尚未获得 F3 原生回归结论。

F3 解锁后限定回归已通过，来源见 `docs/evidence/WP-09-regression-F3-resumed.json`。

## R_PASS_4（只读，正式复核中）

固定 Sdh5qG 构建，先运行五分钟连续缩放，再收齐同构建恢复、四场景、拖动及最终视觉。所有门槛不变，未完成前不宣布 NO_FINDINGS。

### R_PASS_4 结论

Sdh5qG 的静态/动态异常恢复、motion 和 style 限定回归通过。正式缩放 `WP-07-resize-20260913T103146Z` 的 CPU 中位数为 16.44%，仍未满足 15%；其余指标通过（30 FPS、间隔 P95 33.33325 ms、GPU P95 1.79529 ms、最大延迟 82.83317 ms、稳态内存通过）。不能声称 F3 已降低或解决 CPU 问题。整批 `WP-09-verification-20260913T103146Z` 提前结束为 INCOMPLETE，其他正式场景和最终矩阵未执行。

R4 结果：FINDINGS，R09-05 继续开放。接下来仅允许针对该 finding 的 F4，之后 R5 为最终只读门禁；若仍有 finding 必须 BLOCKED_STALLED，不增加第 6 次 R。

## F_PASS_4

针对透明承载层的冗余绘制：FrameSurface 使用 AppKit layer backing 与 onSetNeedsDisplay，成功 Metal 材质期间复用透明承载内容；carrier/fallback 尺寸变化仍显式标记重绘。此语义由本机 SDK NSView.h 的 layerContentsRedrawPolicy 注释核对，模糊与光效仍由原有 Metal 子视图独立绘制。窗口鼠标穿透状态相同时不重复赋值。F3 隐藏 fallback 不更新布局的处理保留；撤回未带来预算通过的延后 AppKit display，恢复原呈现顺序。没有降低帧率、尺寸、日志记录数量或验收门槛。先做限定回归，之后按规约进入 R5。

按用户要求补充 [性能优化依据与论坛对照](../性能优化依据与论坛对照-WP09.md)，区分 Apple 机制说明、历史论坛/作者实验和本机数据；F4 短测 CPU 10.9405% 仅作方向证据，正式预算仍待 R5。

F4 限定回归通过，来源见 `docs/evidence/WP-09-regression-F4.json`。

## R_PASS_5（最终只读门禁，正式复核中）

固定 RrZwDN 构建，先复核五分钟缩放 CPU，再收齐同构建所有正式场景、恢复与最终视觉。所有预算保持原值；若仍有 finding 则 BLOCKED_STALLED，不再自动新增 R/F。


### R_PASS_5 结论（2026-09-13）

FINDINGS → BLOCKED_STALLED。固定 RrZwDN 正式缩放 300 样本，CPU 中位数 15.7364%，超过 15%；FPS 30、间隔 P95 41.666625 ms、GPU P95 2.97308 ms、延迟 P95/最大 57.83338/82.83325 ms 与稳态内存通过。R09-05 未关闭。结果见 [本次正式缩放](../evidence/WP-07-resize-20260913T104528Z/result.json) 与 [最终报告](../最终验收报告-WP09.md)。后续正式场景未执行，当前构建完整最终视觉未完成；没有增加第三次完整视觉判断。四次 F 已用尽，不自动启动第六次 R 或下一工作包。文献支持优化假设但不能覆盖本次 CPU 失败。

### 后续方案（未启动）

按用户“现制定修复方案，search 文献和论坛资料进行推理”交付 [缩放 CPU 修复方案 v1.0](../缩放CPU修复方案-v1.0.md)。UP-02 仅为提案，planned_not_implemented；本记录不新增 R/F，不改变本包阻塞状态。执行方案需以后续用户执行指令为准。
