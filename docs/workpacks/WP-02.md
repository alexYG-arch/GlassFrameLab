# WP-02 — 像素输入与屏幕几何约束

开始：2026-09-11。WP-01 已关闭。

## Intent Freeze

- B02-01：外部期望尺寸以显示像素输入，默认/最小 800×100 px，使用目标屏幕 backingScale 转换为 pt。
- B02-02：窗口宽不超过 visibleFrame.width−48 pt，高不超过 visibleFrame.height×0.8；屏幕边界优先，必要时小于默认最小像素值。
- B02-03：尺寸变化保持当前中心，靠边时采用最小位置修正；移动后不跳回旧中心。
- B02-04：独立 WindowGeometryController 做 75 ms 尺寸去抖、重复提交消除及屏幕变化处理；不实现内容测量。
- B02-05：提供测试入口验证运行中增大、缩小、边界夹取及恢复；不存在布局循环。

允许修改：几何计算模块、WindowGeometryController、FramePanel 接入、启动参数/显式测试入口、相应测试与证据。范围外：屏幕采样、Metal、内容、材质美化。

无效像素输入回退默认并记录原因；无有效可用显示区域时报告错误。阴影当前关闭，预留边距为零；后续若增加阴影必须计入窗口尺寸。单元检查覆盖 1x/2x 和窄屏；真实外接屏留待 WP-08。

WORKPACK_START: WP-02；验证：纯几何边界测试、构建及真实窗口动态尺寸记录/截图。

## R_PASS 1 — 只读审查

FrameGeometry 处理像素转点、最小/最大约束和靠边位置修正；WindowGeometryController 处理 75 ms 去抖、重复提交消除及显示器通知。`--width-px/--height-px` 为外部入口，`--geometry-probe` 为动态测试模式。内部内容未实现。

`WP-02-checks.log` 10/10 检查通过，包含 1x/2x、负坐标窄屏、超限、无效输入及增大/恢复。真实运行 `WP-02-geometry-20260911T032024Z/result.json` 验证默认800×100 → 1600×300 → 当前屏幕上限 → 800×100 px；只有4次有效提交，重复请求未增加提交。已查看增大与恢复后的真实截图，框体完整，默认回到原位置。

重复签名测试暴露 iCloud 文件提供程序持续重加 app 元数据，已改为在 `/private/tmp/glassframe-build.*` 内签名并严格验证，再输出项目内启动链接与完整 ZIP。`WP-02-build.log` 签名有效，ZIP 完整性检查通过；源代码未迁移。此修改仅为恢复当前包可重复构建。

所有 B02 atom 为 IMPLEMENTED_VERIFIED（1x/窄屏为逻辑测试；本机2x为真实窗口）。没有开放 finding。外接屏实机和采样随几何更新属于 WP-03/08，不以本包通过替代。

```text
WORKPACK_CLOSE
id: WP-02
status: NO_FINDINGS
validation: PASS
visual_check: PASS（动态框体）
performance_check: NOT_REQUIRED
open_findings: NONE
```
