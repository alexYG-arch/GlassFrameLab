# WP-08 — 降级与环境兼容


## 本版验收环境调整（用户确认，2026-09-11）

用户明确选择“本版限定当前本机环境，其余环境后续验证”。本版关闭与 WP-09 的验收环境限定为 Apple M4、macOS 26.5.2、内置 2x 屏；真实锁屏冷启动/解锁仍在本版验证范围。macOS 13、外接与跨屏、非 2x 和实际窄屏转为后续环境验证项，状态保留 `BLOCKED_VERIFICATION`，不写为通过或不适用。部署目标 macOS 13 不等于已验证最低系统兼容。此明确范围变更覆盖本版环境门禁，未放宽视觉、性能预算或代码检查要求。

状态：CLOSED，R_PASS_3 为 NO_FINDINGS（用户确认的本机范围）。前置 WP-07 已在 R_PASS_4 达到 NO_FINDINGS。

## Intent Freeze

- B08-01：无录屏权限、启动失败或 Metal 不可用时显示公共 API 的系统材质 fallback，保留主体尺寸、圆角、透明外边距、拖动与右键关闭，不出现不可发现的透明空洞。
- B08-02：依据 ScreenCaptureKit 的 blank/suspended/stopped 状态和流错误处理采样不可用，清除旧样本并隐藏旧材质；不凭全黑像素推断受保护内容，不绕过权限或内容保护。有效样本恢复后替换 fallback，无高频失败重试。
- B08-03：显示器、缩放或显示器几何变化后重新绑定捕获，遵守像素口径和单显示器约束；旧流/旧几何不覆盖最新意图。降级阶段尺寸输入和拖动仍生效。
- B08-04：真实锁屏冷启动/解锁、全屏/Space、最低 macOS 13、外接屏和非 2x 分别记录真实证据。缺环境明确 BLOCKED_VERIFICATION，逻辑测试或注入场景不冒充实机结果。
- B08-05：新增 fallback 不建立常驻动画或高频轮询；捕获/GPU/隐藏回收及原有生命周期回归有效。

范围：FramePanel/FrameSurface、LocalCaptureService、RealtimeGlassController、WindowGeometryController 的显示器交接、开发探针与测试、相关文档。保持当前 GlassStyle 和成功采样时的 Metal shader 不变。

WORKPACK_START: WP-08。先实现本机可执行内容，再独立 R/F；最多 5 次 R，R5 只读。尚未作任何环境验收结论。

## R_PASS_1（只读，验证收集中）

已取得：T2XhfQ 的注入权限拒绝、blank/suspended/stopped、流中断和同屏重绑定、隐藏恢复、降级拖动及菜单关闭通过；实际降级与恢复截图已检查。EyLSO9 的独立应用原生全屏 Space 进出通过；临时独立签名身份得到真实 system_preflight permission_required，显示 native fallback，未更改已有权限。后续 6ZRIbR 补齐 Metal 初始化失败注入与启动 ready 门控，正在收集同构建回归。

- R08-01 / B08-03：跨屏移动的 prepareTransition 目前仍按旧窗口显示器准备两端 union，再在 stage 时重绑定；虽然最终会切换，但会不必要地扩大旧屏采样。应将目标显示器传入准备步骤，跨显示器时直接走 fallback 交接与新流绑定。
- R08-02 / B08-03：显示器 backing 变化中断活动动画时，cancelAnimation(preserveSize:true) 从旧点尺寸乘当前新 backingScale 回算像素，可能改变原像素口径。应保留最后已提交几何记录的 actualPixels，再由目标显示器换算。

以上为代码路径检查发现，尚非真实跨屏实测。macOS 13、外接/非 2x、真实锁屏冷启动后解锁及真实受保护内容仍需分别记录环境证据；不以注入测试替代。R1 待本机固定构建回归结束后汇总结论，再进入对应 F1。

### R_PASS_1 结论

6ZRIbR 构建/签名、18/18 支持模块和 6/6 生命周期检查通过。`WP-08-no-metal-20260911T111702Z`、`WP-08-compat-20260911T111715Z`、`WP-08-fullscreen-20260911T111814Z`、`WP-08-permission-20260911T111838Z` 均通过；默认静态与 20 次恢复 `WP-06-runtime-20260911T111853Z` 通过，静态只合成/模糊一次。motion `112124Z` 通过；缩放短测 `112140Z` 为 29 FPS、P95 41.666625 ms，仅为回归诊断，不替代五分钟预算。

- R08-03 / B08-02、04：仍需用公共 NSWindow.sharingType.none 的独立窗口尝试真实保护属性场景，并如实记录系统是否实际隐藏其内容。该用例不代表 DRM 播放器；如本机不支持相应保护信号，不以注入测试冒充实测。

R1 结果 FINDINGS（R08-01～03）。环境缺口继续保留，不宣布整体关闭。

## F_PASS_1

仅处理 R08-01～03：把目标 NSScreen 传入采样准备，跨显示器/缩放绑定不一致时直接进行降级交接；中断动画时从最后已提交的 actualPixels 保留像素尺寸；增加独立保护属性窗口的有限测试。成功采样的 Metal shader 和 GlassStyle 不变。

## R_PASS_2（只读）

固定构建 `aDPLug`。R08-01 的目标显示器前置检查和 R08-02 的最后已提交像素保留已落实；当前屏幕的 motion、缩放诊断、异常/恢复、全屏及最大尺寸 20 次恢复通过，代码路径复核未发现新的实现问题。实际跨屏/非 2x 行为仍不能由本机同屏结果替代。

R08-03 已取得公共 NSWindow.sharingType.none 的实际对照：彩色测试窗口内容不再进入捕获结果，应用继续显示可采样背景且没有崩溃。观察脚本保持 OBSERVATION_ONLY，独立视觉判定仅对该公共 API 用例通过，未把它升级为 DRM 播放器验证。

当前构建的真实未授权身份、Metal 失败降级及相关截图已复核；18/18 支持模块、6/6 生命周期检查通过。详细证据和边界见 [兼容与降级验证](../兼容与降级验证-WP08.md)。R08-01～03 的实现/指定本机用例问题已关闭。

R2 当前结论：BLOCKED_VERIFICATION。无开放本机实现 finding；仍缺真实锁屏后冷启动解锁、macOS 13、外接与非 2x/实际窄屏环境。尚未宣布 WORKPACK_CLOSE，未开始 WP-09。等待用户配合或明确本版环境验收范围；未经确认不将缺环境改为不适用。

## R_PASS_3（只读，本机范围）

用户明确将本版验收限定为当前本机；后续设备环境继续保留未验证，不以本版关闭宣称跨版本或跨屏兼容。

真实锁屏冷启动证据 `WP-08-real-lock-20260911T114414Z` 通过：公开 loginwindow 信号出现后才启动产品，53 个锁屏期间记录均为 stopped、0 capture_start_attempts、0 GPU 提交、0 retained buffers；解锁后捕获启动一次，一次模糊/合成，有效材质恢复。已独立查看 restored.png：彩色背景模糊连续，完整 800×100 px 主体可见，圆角平滑、没有黑色空洞。未截取锁屏界面。

前一轮 `114204Z` 保留 BLOCKED_VERIFICATION 原始结果：运行恢复数据存在，但人工截图确认超时。后续修正有限测试脚本，在公开解锁信号稳定且有效材质恢复后自动截取测试窗口；产品构建未变。

复核 R08-01～03、当前构建异常/恢复和图像证据，无开放本机 finding。性能短测仅是回归，完整当前构建和效果开关对照留到 WP-09。

结果：NO_FINDINGS。本机范围内 WORKPACK_CLOSE: WP-08。允许进入 WP-09；macOS 13、外接与跨屏、非 2x/实际窄屏继续列为后续 BLOCKED_VERIFICATION。
