# WP-01 — 无系统装饰的跨应用框体

2026-09-11 Intent Freeze，承接用户最新确认。WP-00 已关闭。

| Atom | 目标与验收 | 修改边界 |
|---|---|---|
| B01-01 | 默认使用无标题栏、无红黄绿、无外围底板的 NSPanel；整个可见区域为框体 | FramePanel、main |
| B01-02 | 默认 800×100 显示像素；本机 2x 换算为 400×50 pt | FramePixelSize、FramePanel |
| B01-03 | 右键菜单仅需“关闭”，关闭最后一个框体后程序退出 | FrameSurface、main |
| B01-04 | 窗口透明、floating、不因失去焦点隐藏；记录普通应用/Space/全屏测试 | FramePanel、验证记录 |
| B01-05 | WP-00 校准窗口保留为显式 --calibration；既有基线脚本不被默认模式变化污染 | LaunchOptions、measure_baseline、README |

当前包不采样屏幕、不实现 Metal、不使用系统材质冒充自定义玻璃。可以绘制轻量临时透明轮廓以定位测试窗口，须明确其不是最终材质。框体内部不放标题、说明文字或按钮。复杂尺寸约束属于 WP-02；背景模糊属于 WP-03/04。

默认尺寸单位为显示像素，布局用 px/backingScale 换算，低于1的无效缩放输入不能被静默接受。无显示器时报告错误，不创建无效窗口。退出会停止测量计时器；本包没有捕获循环。

WORKPACK_START: WP-01；验证：构建、像素换算/参数检查、默认框体截图、右键关闭、跨应用/Space/全屏行为记录。

## 审查记录

### 实现与运行检查

- 默认模式改为 FramePanel，旧校准模式仅由 --calibration/--animate 启用；基线脚本已显式选择校准。
- 实机发现系统初始最小高度将框体撑至 51 pt/102 px，已通过清空 minSize/contentMinSize 并提交实际 contentSize 修正。当前截图严格为 800×100 px。
- 签名前将 app 根目录的 Finder 元数据清理放到最后，避免 iCloud 文件提供程序在扫描子文件期间重加根元数据；本轮构建通过。不改变来源或权限属性。
- CUA 右键菜单显示“关闭”，点击后实例在约 68 秒退出（设定自动退出为 180 秒），退出码 0；证据 `WP-01-run-v1/termination.json`。该运行仍为高度修正前版本，菜单实现之后未变化。
- 普通应用叠加截图 `WP-01-overlay-v2/finder-composite.png` 实际背景为 Codex，不是 Finder；按实际观察记录普通应用叠加通过。
- Finder 全屏尝试受 CUA 切回应用桌面影响，相关图片不作为跨应用全屏通过证据；只读窗口查询发生在测试定时退出后，同样不作通过证据。Finder 已恢复测试前非全屏状态和原标签页。
- 首次原生生命周期复合截图包含遗留实例，已添加单实例检查并关闭本项目遗留测试进程，重新取证。

### R_PASS 1 — 当前实现只读审查

| Atom | 结果 | 证据 |
|---|---|---|
| B01-01 | IMPLEMENTED_VERIFIED | 无系统标题栏/按钮的真实截图，FramePanel 透明承载，内部无文字 |
| B01-02 | IMPLEMENTED_VERIFIED | `WP-01-frame-20260911T030558Z/result.json`：实际窗口及截图均 800×100 px，2x 下 400×50 pt；1x 换算逻辑测试通过 |
| B01-03 | IMPLEMENTED_VERIFIED | CUA 实际右键菜单及退出记录，未将定时退出当作菜单关闭 |
| B01-04 | IMPLEMENTED_VERIFIED（当前测试范围） | 普通 Codex 背景叠加；`WP-01-lifecycle-20260911T031424Z/result.json` 与截图验证显示、隐藏、移动及同应用原生全屏 Space 进入/恢复 |
| B01-05 | IMPLEMENTED_VERIFIED | 7/7 检查，默认模式和显式校准路径已分离 |

本轮没有开放 finding。完整第三方全屏应用、外接显示器和 macOS 13 实机仍属 WP-08 环境矩阵，不将当前结果扩大为全环境通过。当前细轮廓是承载层测试外观，未通过玻璃材质验收。

```text
WORKPACK_CLOSE
id: WP-01
status: NO_FINDINGS
validation: PASS
visual_check: PASS（窗口承载层）
performance_check: NOT_REQUIRED
open_findings: NONE
```
