# UP-02 — 缩放 CPU 归因与限定修复

状态：CLOSED_WITH_KNOWN_ISSUE，本机版本带已知问题验收已闭合。用户已明确授权“执行”[修复方案 v1.0](../缩放CPU修复方案-v1.0.md)。本包为授权后的新范围，不修改 WP-09 的历史 BLOCKED_STALLED，不执行其 R6。

WORKPACK_START: UP-02。

## Intent Freeze

- U02-01：量化忙 CPU 与等待，按 H1～H5 证据选择最小修复，不凭论坛个案认定根因。
- U02-02：关闭 R09-05；保留 800×100 物理像素、原光效/帧率/延迟/内存预算和完整记录，不推迟工作冒充通过。
- U02-03：固定候选执行 A-B-B-A 方向对照、两次正式缩放及同构建完整本机验收。
- U02-04：只有相应测试通过并完成真实图像审查才给出本机验收结论；其他环境仍未验证。
- U02-05：最多 R1～R5 / F1～F4，最终视觉最多三次；候选无证据或轮次耗尽时明确停止，不无限试验。

本包不新增业务 UI、第三方依赖、系统全局配置或 CUA 指针。预算沿用 WP-00，源文件范围遵循方案 §7。

## P0 / R_PASS_1

已保存 RrZwDN 失败基线 ZIP 与修改前源码，见 `docs/evidence/UP-02-baseline`。ZIP 可执行文件与原实测路径一致。当前先采原构建 CPU 归因数据；profiling 数据不作为正式预算。

### R_PASS_1 结论

FINDINGS：R09-05 仍开放，现有 sample 不能量化同步阶段。原构建 CPU Profiler 已记录 75 秒，14,919 条 Running 样本；最近应用调用帧的运行周期权重中 Diagnostics.writeJSON 约 6.423%，CA 内容更新包含路径约 20.488%，nextDrawable 包含路径约 17.255%。这些是采样周期权重，非单核 CPU 百分比，包含路径不可相加。证据见 `UP-02-profile-cpu-profiler-20260913T110629Z/cpu-summary.json`。

原始 trace 的自动环境元数据不纳入共享报告，导出 CPU 表后清理临时原始 trace；证据目录保留去除环境字段的 TOC 与 CPU 表。后续诊断进程仅继承必要环境变量。

## F_PASS_1：限定计时探针

仅添加 GLASS_CPU_DIAGNOSTICS=1 时启用的同步主线程 CPU/墙钟计时，覆盖 JSON 编码/写入、setFrame、Surface layout/draw、nextDrawable、CA commit/flush、捕获配置构造；不跨 await，30～120 秒窗口，每阶段最多 20,000 条，报告溢出。默认关闭，不改变布局、光效、记录数或预算。R2 以实际阶段数据决定修复方向。

F1 构建 exzc9Q 成功。首次阶段测试遇到 login_window，启动采样次数为 0，按环境中断保留 INCOMPLETE；未进入候选判断。用户解锁回复待到达。归因进度见 [记录](../缩放CPU归因-UP02.md)。

F1 自动检查通过：22/22 LabSupport、6/6 生命周期，见 `UP-02-checks-F1.log`。再次只读核对桌面仍为 loginwindow；当前等待解锁后继续阶段诊断，不将环境中断记为修复失败，不选择未经计时支持的候选。

## R_PASS_2 结论

用户解锁后，同一 F1 诊断正常结束：`UP-02-diagnostic-F1-stages-resumed-20260913T115726Z`，90 秒 CPU 中位数 14.87095%，仅作诊断。CACommit 2,700 次/线程 CPU 1,802.37 ms，NextDrawable 2,700 次/1,446.40 ms，JSONWrite 2,427 次/1,373.55 ms，JSONSerialize 103.57 ms，PanelSetFrame 832.52 ms，SurfaceLayout 1.75 ms；SurfaceDraw 没有窗口内调用。所有探针零溢出。

FINDINGS：R09-05 未关闭；H1 主要是写入而非格式化，H3 配置构造仅 15.89 ms，不作为优先修复。H2 存在无重复 draw 但 CA 缓存内容更新的成本，选择空透明 layer 更新路径作为最小候选，效果仍须 A-B-B-A 验证。不能将 CACommit 全部成本归因于 FrameSurface。

## F_PASS_2：透明承载层空内容路径

移除 F1 临時計时探针，保留其 patch/构建/原始诊断。只修改 FrameSurface：在成功 Metal 且无 carrier/fallback 时使用 wantsUpdateLayer/updateLayer，将自身 contents 清空；carrier/fallback 继续原有 draw 路径、尺寸更新与外轮廓。相应机制核对 Apple wantsUpdateLayer 文档。日志记录、采样调度、几何和呈现顺序、shader、预算不变。接下来固定 A=RrZwDN、B=候选，按预定 A-B-B-A 各 30+90 秒执行，结果全部保留。

### F2 对照结果

预定 A-B-B-A 已完整结束。A1/A2 CPU 15.2173% / 14.93845%，B1/B2 13.58255% / 13.07375%；两组均值差 1.749725 个百分点，超过基线两次差 0.27885。四次 FPS 中位数均 30，间隔 P95 约 41.6666 ms。方向支持该候选，但不是正式预算结论。日志数量随真实提交变化，未禁用记录。已查看 B1 原生缩放截图，未见毛边或底板，属于局部诊断观察，不计完整最终视觉判断。

## R_PASS_3（只读验证）

固定 9mXYWr，22/22 支持模块和 6/6 生命周期检查通过。产品与基线只有 FrameSurface 的 6 行增量，临时阶段探针全部撤回。开始同构建兼容/几何/动态回归、两次正式缩放与完整验收；所有预算不变。

### R3 已完成的正式缩放

第一次 `WP-07-resize-20260913T121329Z`：CPU 中位数 13.79705%，GPU P95 1.65925 ms，FPS 30，间隔 P95 41.666625 ms，延迟 P95/最大 66.16654/74.50021 ms。第二次 `WP-07-resize-20260913T121904Z`：CPU 13.8288%，GPU P95 1.26108 ms，FPS 30，间隔 P95 33.333334 ms，延迟 P95/最大 66.16675/82.83333 ms。均为 300 个正式 CPU/RSS 样本，稳态内存通过；未平均两次结果来判定。

R09-05 的缩放 CPU 验证已满足，仍待同构建其他性能/恢复和最终视觉收齐，当前不宣布工作包关闭。

## R_PASS_3 结论与 F_PASS_3

FINDINGS：R09-05 两次缩放 CPU 通过，但同构建最大尺寸出现 U02-06 稳态 RSS 超限，R3 整批 INCOMPLETE，后续拖动/视觉未执行。最大尺寸斜率 2.72570466 MiB/min、首末分钟差 9.171875 MiB；保留原失败结果。

第 164→165 秒 RSS 82.5625→91.65625 MiB，恰与首次完整帧像素相等事件重合：captured/changed 从 4819/4819 变为 4849/4848。随后约 166 秒持平，纹理分配始终 1、最新缓冲 1。该证据支持首次全量比较触发 CPU 页面驻留的解释，不足以宣称已证实持续泄漏或精确底层分配。

F3 根据 ScreenCaptureKit 的 dirtyRects 元数据增加保守去重：只有相同 mapping 且明确、合法的零面积变化矩形才复用旧像素；缺失、未知或非空变化仍精确比较。保留时间戳刷新及下一有效帧通知。添加常数计数器供验证，不预热触碰内存、不更改 fixture、warm-up 或预算。当前仍为候选，需独立验证。

## R_PASS_4 结论

FINDINGS：最大尺寸正式门槛数值通过，但元数据候选只避免 1 次比较，仍有 119 次像素相同的完整扫描；第一次在第 1 秒已出现，RSS 首样本已约 91.78 MiB。无法证明消除了 R3 的晚发生首次读取，候选不晋级。完整最大尺寸测量结束后由本任务主动中断后续场景，非用户锁屏；整批 INCOMPLETE。原始数据完整保留。

## F_PASS_4：撤回未证实候选

保存 NSIpCN 的 patch/ZIP，撤回 F3 的元数据路径、计数器及新增测试，恢复 F2 的 9mXYWr 包。仅保留已经两次正式缩放验证的承载层 6 行修复。未放宽内存预算或更改 fixture。回退后 22/22 支持模块、6/6 生命周期通过。

## R_PASS_5 最终复核

BLOCKED_STALLED。只读比对确认源码精确等于原基线加 F2 的 6 行 patch；ZIP 内可执行文件与 9mXYWr 实测路径一致。R09-05 缩放 CPU 验证已满足，U02-06 最大尺寸 RSS 仍开放；原 R3 失败不能被 R4 的提前读取结果覆盖。当前没有完整产品验收，也没有最终视觉判断。

本包 R1～R5、F1～F4 已结束，不再执行 R6。详见 [执行结果](../缩放CPU修复与最终验收-UP02.md)。已制定 [UP-03 方案](../最大尺寸RSS复核与后续修复方案-UP03-v1.0.md)，尚未授权执行；其他环境未验证。测试窗口、fixture 和临时防休眠均已清理。

## 用户授权的验收收尾（2026-09-13）

用户明确接受“将这项内存偏差列为本机版本的已知问题，延期排查”，要求补完当前构建拖动测试和最终视觉验收，再以带已知问题验收闭合。该授权覆盖此前 R5 停止后的本次验收收尾；不重写原 R/F 记录，不启动 UP-03，不修改产品代码。U02-06 按 [已知问题记录](../本机已知问题.md) 保留原数值失败，其余门槛不变。当前收尾结果待正式拖动及真实截图审查完成。

## 授权收尾完成

当前固定 9mXYWr 的正式拖动 CPU 中位数 13.73425%，GPU P95 1.75225 ms，呈现 30 FPS，间隔 P95 33.33325 ms，延迟 P95/最大 66.16646/132.83275 ms，稳态内存通过。22 组原生矩阵和 3 个实时背景均完成直接图像审查，最终视觉为 NO_VISUAL_FINDINGS。

合并同构建证据的 47 项检查中 46 项通过，唯一原失败 large_memory_stable 对应用户接受的 U02-06。原阈值、历史失败和未证实根因保留；原 R5 未被追改为技术通过。当前产品验收决定为 CLOSED_WITH_KNOWN_ISSUE。

[验收记录](../evidence/UP-02-local-acceptance-20260913T132602Z/acceptance.json)、[完整报告](../缩放CPU修复与最终验收-UP02.md)、[已知问题](../本机已知问题.md)。本次没有产品代码修改，ZIP 与实测二进制一致；测试窗口和临时防休眠清理。UP-03 延期未启动，其他环境未验证。
