# WP-06 — 玻璃边缘视觉升级

状态：已关闭（R_PASS_3，NO_FINDINGS）；前置 UP-00/UP-01 已关闭。依据升级计划 v1.0 和主规约 §1.4.1。

## Intent Freeze

- B06-01：沿整个圆角轮廓绘制淡、深色、零偏移的外扩散；透明边距内平滑消失，不新增模糊 pass 或中间纹理。
- B06-02：细边内侧有更宽且向中心衰减的非均匀内发光；周长方向上强弱和宽度连续变化，保留清透中心，不画固定背景色斑。
- B06-03：模糊、饱和度、亮度、主题色/透明度、圆角、细边、内外光晕集中配置；开发入口可分别关闭三层；参数变化不能被静态帧去重漏掉。
- B06-04：纯白、浅灰、浅暖、浅冷、深色、多色背景及默认/较大尺寸真实截图验证；光晕无裁切，边缘不消失、不脏灰、不明显过曝，显式前景标记清晰。
- B06-05：保持 800×100 px 主体、局部采样、队列/纹理/在途上限和静态不重复渲染。记录新增 drawable 面积与五分钟静态/动态/最大尺寸性能，沿用冻结预算，不能沿用旧构建结果。

范围：集中样式、最终 Metal 合成、材质失效信号、开发配置/显式背景夹具、视觉与性能验证。不新增产品内部内容，不更改已验证的窗口交互。

WORKPACK_START: WP-06。

## 初始实现与证据

集中 GlassStyle 和 JSON 开发配置；外扩散、内发光、细边合并在一个 fragment 合成阶段，无新增模糊 pass 或中间纹理。配置版本触发重绘，颜色/边缘变化复用当前模糊结果；sigma 纳入采样映射并使旧帧失效，避免裁切区域相同时漏掉重配置。

`WP-06-visual-20260911T061601Z` 误运行旧构建，缺 blur_encodes 字段而失败，不能作为升级证据。`WP-06-build-final.log` 随后正确构建/签名；`WP-06-visual-20260911T061639Z` 保存 22 组真实截图，结构检查通过（每组一张中间纹理、一次模糊及一次呈现提交，默认总窗口 824×124 px、主体 800×100 px，四角透明）。截图构建为 fpvQ3U，之后的 sigma 失效修正尚待最终构建验证。

## R_PASS_1（只读）

已查看白、灰、暖、冷、深、多色及较大框体和清晰前景；浅色背景轮廓可辨，外扩散零偏移且无矩形裁切，内光带有方向性。仍有 finding：

- R06-01 / B06-04：深色纯背景上的整体提白偏强，大框体看起来像灰色底板；需降低集中 tintOpacity，保留内光带与细边的局部对比。
- R06-02 / B06-03/05：需要最终构建的静态配置切换验证，证明每次变化重绘但不重复模糊，不因配置变化自激升频；新建参数的日志口径/使用文档需完整。
- R06-03 / B06-05：升级构建的五分钟静态/动态/最大尺寸、20 次恢复和分层成本对照尚未完成，旧构建预算不能替代。

结果 FINDINGS。

## F_PASS_1

仅处理 R06-01～03：降低整体提白，保留两种核心光晕；补齐参数切换与文档，固定构建做视觉复核及性能/回收测量，不提高预算。


F1 固定构建：`/private/tmp/glassframe-build.B1XFaX/GlassFrameLab.app`。默认 tintOpacity 调为 0.008，新增内发光颜色与其他参数一起集中配置；默认仍为白色内光带。最终构建、签名与 15 项支持模块/6 项生命周期检查见 `WP-06-build-F1-final.log`、`WP-06-checks-F1-final.log`。

- `WP-06-visual-20260911T062811Z`：最终构建 22 组真实截图，结构检查通过。已视觉检查白/灰/暖/冷/深/多色、较大尺寸、分层关闭和清晰前景。较早 `062129Z` 是 B1XFaX 之前的 DEMGB0 构建，保留作调参历史，不混作最终构建证据。
- `WP-06-style-updates-20260911T062313Z`：静态背景下 3 次配置变化后，累计 4 次合成、1 次模糊、1 次纹理分配；最终样式版本一致，15 Hz，不因配置切换持续升频。此场景复用旧采样，因此样本年龄不计入动态延迟预算。
- `WP-06-runtime-20260911T062328Z`：40 秒静态及 20 次显示/隐藏、组合暂停恢复均通过；与最终构建及背景夹具身份一致。正式五分钟测量正在运行，尚未作 R_PASS_2 结论。

正式四场景记录 `WP-06-performance-20260911T063012Z` 的静态场景在约 307～309 秒收到 18 个像素不同的新帧，对应 18 次额外模糊/合成；来源未确定，不能计作静态五分钟通过，原始数据及 input-stability-finding.json 保留。由于 changed_frames 与 submissions 一致，当前证据没有显示对同一个有效帧反复提交。安排同构建、同夹具、同场景一次完整定向复测；不删除原失败。

R06-03 测量精度修正：CSV 保存九位小数的秒时间戳，直接以大 uptime 的二进制浮点相减会将精确 50 ms 写成 50.00000004656613 ms。分析器改用 Decimal 在原始精度下先相减，再转换毫秒；默认动态精确 nearest-rank P95 为 50.000000000 ms。没有增大门槛或加入 epsilon。已检查等于门槛和高/低 1 纳秒三种边界，见 WP-06-timing-precision-check.log；原始时间戳不改写。

四场景最终记录已返回 FINDINGS（静态输入变化导致 no-repeat 检查失败）。完整默认动态预算通过：27 FPS、GPU P95 2.75375 ms、帧间隔 P95 50 ms、延迟 P95 49.49996 ms / max 66.16654 ms、CPU 5.8332%。最大尺寸 2904×1408 px，29 FPS，GPU P95 3.38117 ms，CPU 6.33195%，五分钟内存稳定。

光效关闭仅为同构建测量对照，不作为另一个已验收质量档：该组中位 27 FPS，但出现 max 416.16675 ms 呈现延迟和 4 个未呈现 drawable；帧间隔 P95 50.000041 ms。开关差值包含运行间波动，不能据 GPU/RSS 的负差值声称光效降低功耗。当前 shader 的零强度开关也不保证驱动删除所有对应算术，故只报告整条管线在开关状态下的实际差值，不将其当作纯新增指令成本。

## R_PASS_2（只读）

R06-01、R06-02 已由最终构建视觉与样式更新证据关闭。R06-03 的默认动态、最大尺寸稳态、默认 20 次恢复与资源预算已满足；静态完整复测 `WP-06-performance-20260911T065305Z` 仍返回 FINDINGS：约 286、291～292、299 秒产生新像素帧，最终 17 次合成，不能宣布静态通过。没有修改阈值，也没有将此复测替换原始记录后声称整组通过。

新增 R06-04 / B06-05：静态输入不稳定的来源尚未识别，需在变化时同时记录实际测试区域、独立背景窗口及窗口层级，定位环境输入或采样变化；不得再做无诊断的盲目重跑。最大尺寸隐藏回收另需补证据。关闭光效对照的延迟异常与成本解释限制继续保留，不将开关对照误写为性能改善结论。

结果 FINDINGS。

## F_PASS_2

仅处理 R06-03/04：补充最大尺寸回收验证，并定位静态输入变化。保持现有 B1XFaX 应用构建，先使用外部只读诊断，不修改产品管线或抬高预算。

F2 环境说明：用户确认测试期间切换到了 Finder，与两次日志中短暂的新像素帧相符；这是静态输入条件受干扰，不能从额外提交推断为重复渲染故障。外部输入诊断工具已编译准备，但尚未运行；不将工具准备当作根因实测证据。已告知用户下一轮需约 6 分钟固定背景，安排一次同 B1XFaX / V6gP4C 构建的静态受控验证，前两次完整数据继续保留。

最大尺寸回收：`WP-06-runtime-20260911T070021Z/cycles` 通过，实际主体 2904×1408 px，组合暂停/恢复及 20 次显示/隐藏有效，最终无保留采样帧或 GPU 在途工作。


F2 受控静态证据 `WP-06-performance-20260911T070408Z` PASS：预热 30 秒后完整 300 个秒样本，CPU 中位 0.2111%，RSS 中位 58.875 MiB，对照基线 50.078125 MiB；稳态零新增 GPU 命令，最终累计 1 次模糊/合成/纹理分配。与之前动态、最大尺寸和生命周期证据采用相同不可变应用与夹具构建。

## R_PASS_3（只读）

B06-01～05 IMPLEMENTED_VERIFIED。复核最终 22 场景、参数切换、默认及最大尺寸回收、受控五分钟静态、原有效五分钟动态/最大尺寸和精确时间口径。所有默认预设硬预算通过，来源与局限见 `docs/evidence/WP-06-budget-consolidated.json`；原受干扰静态记录和光效关闭参考组的延迟异常保留，不将它们提升为通过场景。没有精确瓦数或最低系统/外接屏结论。

R06-01～04 已关闭；当前范围无开放 finding。结果 NO_FINDINGS。

WORKPACK_CLOSE: WP-06。允许进入 WP-07。
